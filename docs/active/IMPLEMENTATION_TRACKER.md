# swiss-shopping-mcp Implementation Tracker

## Open items — START HERE (updated 2026-08-06)

Everything below this section is history. `## Next tasks` further down is an
older list that is now almost entirely struck through — do not mistake it for
the current backlog. These are the only things actually open:

**Where 2026-08-06 left off.** Closed that day: **11** (filter recall 0.768 →
0.871, now gated offline against a pre-filter pool), **8** (production's web
tier measured *alive* — `ddg` 43 requests, 0 challenges, 0 breaker opens, read
from `GET /api/metrics` on the Pi), and the software half of **12**. Baseline is
green: 932 tests, lint 0 errors, pool recall 0.871, pool P@5 0.965, 0 wipeouts.
*(Superseded the same day by the relabel in item 2c — the current baseline is
943 tests, lint 0 errors, fixture P@5 0.953, judged@5 0.996, pool recall
**0.888**, pool P@5 0.945, 0 wipeouts. The label change lowered the numbers as a
ground-truth correction with the ranker untouched; the matcher change in item 2d
then took recall past where it had ever been.)*

**Read before touching relevance:** `CLAUDE.md`'s new *research contract*. The
last session burned an afternoon re-deriving what e-commerce IR calls
**product-type matching**, graded on the **ESCI** scale, having consulted only
antigravity and never the literature. Do the search first.

**Labels fixed 2026-08-06 — that blocker is gone, and it was bigger than the
row below claimed.** The corpus was wrong in *both* directions, and worse on
`Gemüse` than on `Obst`. See item 2c. **The next concrete step is now a matcher
change**, and the corrected ground truth already names two:

1. ~~**`Rüebli` keeps the biscuits and drops the carrots**~~ and
   ~~**`Obst` drops `Sultaninen`**~~ — **both fixed the same day, see item 2d.**
   Recall `ruebli` 0.150 → **1.000**, `obst` 0.916 → **1.000**, `kartoffeln`
   0.859 → **1.000**, mean 0.867 → **0.888**, which is above the 0.871 ever
   measured under the wrong labels.

**What is still open is the other half — precision.** `Rüebli` now *admits* the
carrots (strength 80) but still **ranks none of them in the top 5**: pool P@5
for that query is **0.00**, because every Rüebli cake, biscuit, Rörtli and
marzipan carrot scores **90** off the whole-name bonus and buries them. That is
the same defect family as the hypernym problem in item 2 — the matcher reads the
query word appearing anywhere in a name as though it named the product's *type*.
`Früchtequark Aprikose` is a quark; `Cake Rüebli` is a cake. The judge now knows
this (item 2c's `related` list); the **matcher does not**, and teaching it is the
next piece of work.

The hypernym band experiment can also be retried honestly now — under the
corrected labels `obst` scores P@5 **0.60**, not the 0.80 the old labels
reported, so a real fix has somewhere to show up.

**Waiting on the owner, not on code:** the Argon ONE V2 jumper in item 12.

| # | Item | Where |
|---|------|-------|
| 2 | **Phase 6 — search relevance.** **Update 2026-08-06 — the stain remover is gone, via a food/non-food boundary rather than the category signal this row proposed.** The row's stated next lever was "its category says `Fleckenentferner`, so a category-contradiction signal is the next lever". Measuring first killed that plan: **Migros populates `category` for zero of its 1376 pool products**, while Coop fills 1325, Ottos 848, Aldi 351. Penalising a contradicting category therefore only ever punishes the chains that publish one — antigravity named this a **metadata tax**, and the consequence is worse than the omission: a Coop stain remover would sink while an identically-named Migros one sailed past, so the ranking would encode which vendor has better metadata rather than which product answers the question. **German names carry the function in the noun and do so in every chain** (Migros ships "Allzweckreiniger" and "Flüssig Waschmittel" under an empty category), so `src/util/productDomain.ts` decides the macro-domain from a small closed set of name stems instead. **It was never one product:** measured across the pool, the products still surviving the filter for a food query were two for `obst` (the Dr. Beckmann stain remover and a Dettol soap dispenser named "Gartenfrüchte"), five for `lait-entier` (Palmolive and Malizia liquid *soap* with milk, two oat-milk shampoos) and one for `beurre` (a shea *butter* shampoo) — a whole class of non-food articles named after the food they act on or smell like. **The vocabulary is used symmetrically**, so `Zahnpasta`/`Waschmittel`/`Reinigungsmittel` — themselves golden queries, whose pools hold 113/109/53 correct non-food products — are untouched. **Two corrections the suite forced, both worth keeping.** (i) A first version read "query contains no cleaning stem" as evidence of food, and disqualified every Optimus WC-Reiniger from the brand query **"optimus"**. Absence of evidence for non-food is not evidence for food; brands carry no domain in their spelling. The rule now requires **positive** identification of a food query, and that asymmetry is the safety property: a term missing from an allowlist declines to fire, where a term missing from a denylist silently deletes results — the "Milchdrink UHT" failure. (ii) The judge had a test asserting this exact stain remover was `relevant`, commented "beyond what an identity-only judge can rule out". True of a head rule and false of a domain rule; flipped. **Numbers, stated honestly: recall unchanged at 0.871 with 0 wipeouts, and pool P@5 went 0.969 → 0.965 — a fall.** Isolated by re-running with the judge fix alone: it produces 0.965 on its own, so the entire move is the judge no longer certifying non-food as relevant, and the matcher change costs nothing. The metric cannot show the actual gain, because the freed rank-3 slot refills with another product the labels do not name, and swapping unlabelled junk for unlabelled junk is invisible to P@5. **Still wrong in the same query:** "Obst" now ranks `Hipp Bio Brei Obst & Vollkorn ab 6+ Monate` at **#1** — baby purée, which the row already flagged and which no domain rule can catch because it is food. **Attempted and reverted the same day, recorded so it is not retried blind:** raising the taxonomy-member band from 40 to 85 for hypernym queries, plus a token-count "tightness" test to separate an identity claim ("Mischobst") from a mention ("Hipp Bio Brei Obst …"). It did fix this query — purée gone, `Aprikosen`/`Datteln` in the top five — and pool P@5 held at 0.969, but `matcher.test.ts` caught a real regression: for **"geriebener Käse"**, `Le Gruyère gerieben` (a `kase` member, now 85) overtook the product that actually carries Käse in its category (62). A *modified* query wants the noun; only a *bare* category query wants the members, and the boost did not distinguish them. Reverted rather than threading a `bareHypernymQuery` flag through three signatures on a tired budget. **The real lesson is methodological and is now in `CLAUDE.md`:** this was re-derived from first principles across several exchanges with antigravity while the field's answer sat unread. E-commerce IR calls this **product-type matching** — extract the product type from the title and match *that*, rather than proxying it with token overlap, which is all the token-count heuristic ever was — and grades query/product pairs on the **ESCI** scale (Exact / Substitute / Complement / Irrelevant), under which `Mischobst` is Exact, `Aprikosen` Exact, the Hipp purée Complement and the stain remover Irrelevant. That is the frame for the next attempt. **Fix the labels first:** `goldenSet.ts` calls obst relevant only for obst/apfel/banane/birne/orange/beere/traube/fruit, so `Aprikosen` and `Datteln` score *neutral* while being precisely what was asked for — any P@5 measured on this query is partly measuring a bad label. | `src/util/productDomain.ts`, `CLAUDE.md` research contract |
| 2c | **The golden set was certifying the wrong answers, and the metric was measuring its own labels (fixed 2026-08-06).** Prior art first, per the research contract: e-commerce IR grades a query/product pair on the **ESCI** scale (Reddy et al. 2022, arXiv:2206.06588), whose **Exact** is "relevant for the query, and satisfies all the query specifications" — so for a bare category query the only specification is the category, and `Aprikosen` *is* Exact for "Obst". **The row above framed this as two missing labels. Measured against the frozen pools, it was a systematic defect in both directions.** For `obst`: 15 products labelled relevant, **8 of them not fruit** (Hipp baby cereal, two Früchtequarks, three fruit bars, two smoothies), while `Aprikosen`, `Datteln`, `Feigen`, `Physalis`, `Nektarine`, `Pfirsich`, `Pflaumen`, `Passionsfrucht` and `Pitahaya` sat **unjudged** — 24 after relabelling. **`Gemüse` was worse and nobody had looked:** it scored a **perfect P@5 1.0** on a top 5 holding a sweet-and-sour sauce, a vegan meat alternative and a vegetable juice, with Felix **cat food** ("Gemüse in Gel") also labelled relevant — and it *forbade* `Gemüsebouillon` while *certifying* `Knorr Gemüse-Extrakt`, the same product in two spellings. `Rüebli` was the same defect outside the category bucket: Rüebli biscuits, a Rüebli cake, a carrot-orange-ginger juice and marzipan carrots all counted as the vegetable. **The cause is one piece of grammar read at the wrong scale.** The existing head rule is right that a German *compound* puts its head last (`Him|beere` is a berry, `Obstessig` a vinegar — Williams' Right-hand Head Rule), but a multi-word retail *title* runs the other way: type first, flavour after. `Früchtequark Aprikose` is a quark, so the fruit lemma matches a slot that says nothing about what the thing is, and no lemma list can separate the two readings. **Fixed by naming the type instead of inferring it:** a new `related` list (ESCI Substitute/Complement, matched anywhere, outranking `relevant`, never gating) lets `relevant` enumerate fruit generously without re-admitting every dessert flavoured with one. Rejected: teaching the judge to parse title syntax — this corpus exists to catch the ranker guessing and must not contain a second guesser. **Also added: `judged@5`**, the share of the scored top 5 carrying any label, reported beside P@5. P@5 counts an unlabelled product as non-relevant (the TREC pooling assumption), which holds only while labels cover what is retrieved and which nothing in P@5 announces — that is precisely why the failed band experiment could not be credited for promoting `Aprikosen`. 0.976 → **0.996**; ratcheted, never hard-gated on zero unjudged, because pools are recaptured live and a red build meaning "the catalogue changed" is a red build everyone learns to ignore. One product is left deliberately unjudged (`High Protein Apple-Cinnamon`, whose type is genuinely unstated in its identity). **Numbers, stated honestly — all of it is ground-truth correction, the ranker was not touched:** fixture P@5 0.973 → **0.953**, MRR 0.974 → 0.980, category bucket 0.9 → **0.75**, compound 0.992 → 0.96; pool recall 0.871 → 0.867 and pool P@5 0.965 → 0.941. **The metric got worse because it stopped lying.** 937 tests green, lint 0 errors. **Consulted antigravity** (`gemini-3.6-flash`), which recommended full per-product TREC qrels + ESCI grading and cited Voorhees on pooling, Reddy et al. for ESCI, Williams 1981 for the head rule and Wayfair's WANDS. **Departed from it on cost:** the pools hold **3,397 unique query-product pairs**, so its "bootstrap then review the diff" is ~3,400 hand judgements repeated on every recapture. Put that measurement back to it and it agreed the pattern scheme is defensible, endorsed collapsing S and C on the ESCI paper's own reliability finding (91% four-way agreement vs **>96%** binary Exact/not-Exact, §2.2), and answered the scoping question with a **fallback cascade** — explicit per-product qrels overriding patterns for the few queries where patterns break, rather than two labelling systems. **That cascade is the recommended next architectural step and is not built.** | `src/eval/goldenSet.ts`, `src/eval/relevanceScoring.ts`, `src/eval/README.md` |
| 2d | **The filter was inverted on "Rüebli", and one missing lexical relation caused it (fixed 2026-08-06).** **Why we filter at all**, since it was asked: `productMatches` does five things, and only one is contentious. `maxPrice`/`category`/`tags` are the shopper's own filters and a `price <= 0` drop is data sanity; the fifth is the relevance floor — `calculateMatchStrength === 0` discards the product. It exists because vendor search is recall-oriented and we fan out over seven chains: measured on the frozen pools, "Aproz" returns **41 products of which 1 is Aproz**, "glutenfreies Brot" **98 of which 6** are gluten-free bread. Without a floor the merged list is mostly whatever each vendor chose to promote. **The failure mode is the problem, not the idea:** a filter fails *catastrophically* (the product is unreachable) where a ranker fails *gracefully* (it sits at rank 40) — which is exactly what happened here. **The defect: `HYPERNYMS` conflated two different lexical relations.** It is directional on purpose so that "Apfel" cannot match a banana — correct, because those are **siblings**. But `rueebli` appeared only as a *value* under `gemuse` and never as a *key*, so it expanded to nothing; and since an unrecognised token is mandatory, that did not merely fail to help, it **disqualified every carrot**. Measured: `Rüebli → Karotten` scored **0**, and so did `Karotten → Rüebli`. `Rüebli` and `Karotte` are not siblings — they are the same vegetable in Swiss German and standard German, so the relation is **synonymy**, which is symmetric by definition and cannot reach a different product. **Fixed with a separate `SYNONYM_GROUPS` table**, symmetric, with both directions *derived* from one group so a pair cannot be added one way round — which is precisely how this went missing. Prior art, per the research contract: **ISO 25964-1** keeps the equivalence relationship (USE/UF) formally apart from the hierarchical one (BT/NT); WordNet separates a synset from the hypernym links *between* synsets; Lucene ships `SynonymQuery` distinct from any taxonomy rewrite; Voorhees (1994) is the standard warning about expanding without regard to relation type. **Scored at the direct band (80 name/brand, 60 category), not the hypernym discount (40/30)** — Lucene's `SynonymQuery` rule, and it follows from the meaning: a hypernym member only *partially* satisfies the intent, whereas nothing about `Karotten` is a weaker answer to "Rüebli" than `Rüebli` is. The literal spelling still takes ties through the 100/90 whole-name bonuses. **Deliberately not merged into `CROSS_LANGUAGE_TERMS`**, the obvious home since a dialect word is a translation of a sort: that table also drives `vendorQueryFor`, so an entry there rewrites the query *before dispatch*, and Migros shelves its own products as "Rüebli" — translating it to "Karotte" would lose the very products the entry exists to find. **Results:** `ruebli` recall 0.150 → **1.000** (53/53), `obst` 0.916 → **1.000**, `kartoffeln` 0.859 → **1.000** (the `Herdöpfel` group), mean 0.867 → **0.888**, pool P@5 0.941 → 0.945, **0 wipeouts, no query fell**. `Apfel → Bananen` is still 0 and is asserted by a test, so the sibling protection the directionality exists for is intact. 943 tests, lint 0 errors. **Still open and now visible: precision.** `ruebli` pool P@5 is **0.00** — see the header. | `src/util/matcher.ts` `SYNONYM_GROUPS` |
| 2b | **Phase 6 original notes.** The golden set exists (`src/eval/`, 51 labelled queries, gated in CI) and its **coverage half is now done**: 0.902 → **1.0**, end-to-end P@5 0.894 → 0.98, via query understanding (`src/util/queryUnderstanding.ts`) rather than any ranker change — modifiers now match across inflection and are canonicalised for vendor dispatch, and fully-romance queries are translated before dispatch. Verified per query before re-baselining: none fell, 5 rose from 0. **Still open:** the answered-only P@5 is 0.98 with `Rüebli`→carrot cake at #3 non-gating. **Milchdrink UHT got its actual cause on 2026-08-05, and it was neither vendor churn nor the ranker.** An unrecognised query token is *mandatory* — `strengthForQuery` disqualifies any product missing it unless it is a known `preference` modifier — and `uht` was absent from the modifier table. Migros names all twelve of its products "Milchdrink" with the heat treatment nowhere in the title, so every one scored 0 and was dropped by the adapter's own filter: measured live, Migros returned **0** for "Milchdrink UHT" and **12** for "Milchdrink". Coop and Volg do write "Milchdrink UHT 1.5% IP-Suisse", so the query answered anyway and the golden set scored it **P@5 1.0 while the largest chain in the country contributed nothing** — the gate could not see the defect precisely because one vendor spelled the attribute out. Fixed by classifying `uht`/`ultrahocherhitzt` as `preference` beside `pasteurisiert` (not `constraint`: constraints are the diet/allergen/certification claims a shopper cannot trade away, and getting pasteurised instead of UHT costs shelf life, which is a rank penalty). After: "Milchdrink" scores 60 where it scored 0, "Milchdrink UHT" still scores 100 so full-name vendors still outrank, **"Milchschokolade" still scores 0** so the originally-reported wrong answer is not reintroduced, literal mode unchanged; live recall 0 → 4, browser-verified with the Migros Milchdrinks at ranks 15–18 and Denner and Lidl recovering too. **Generalise before closing this item:** every missing modifier is a silent recall defect of exactly this shape, invisible whenever at least one vendor happens to spell the attribute out, so the modifier table is a denylist against an open vocabulary. The second reported defect (Protein Milch → bread) is still **masked, not fixed**: Migros returns exactly one correct product ("Milch" / brand "Migros High Protein") and Aldi returns zero, its real product living on the unbuilt aldi-now.ch. Both remain armed as gate queries. Read `src/eval/README.md` before starting. | `PWA_UX_FIX_PLAN_2026-07-30.md` §2.10, `src/eval/README.md` |
| ~~9~~ | ~~**PWA UX round 4 — five items from real phone use (2026-08-04).**~~ **All five implemented and browser-verified 2026-08-04.** (a) the loading trace now tapers — five stacked rects sharing one animation, each drawing a shorter dash centred in the same slot at a higher alpha (a gradient or mask cannot do this: both paint in user space, so the fade would stay put while the segment travelled); (b) editing the query mid-search swaps the button to "Neue Suche starten" and the click aborts the running stream — verified by watching stream A close without ever receiving its `done` event; (c) the ETA is a range bounded by what the pending chains are still *allowed* to take, plus the web/merge stages the old estimate ignored, and it names the last straggler instead of counting to zero; (d) the PWA's `limit: 12` is gone (a broad query renders 84 products, measured at 66–80 / ~80KB before the change); (e) the chat's text-form `<tool_call>` leak is recovered server-side by a language-model middleware — `experimental_repairToolCall` structurally cannot catch it, since it only fires once a call has been parsed — and the client now refuses to render tool-call syntax and shows an explicit failed turn with a retry instead of the indicator silently vanishing. **Still open from this round:** the server keeps running the fan-out after a client abort (that is item 4 below). The out-of-order transcript is **closed as not-a-bug (2026-08-05)**: the transcript never sorts by arrival, and mid-flight the composer provably refuses to send (browser-verified, both submit paths, no second request), so the cards under "Hello?" were the reply *to* "Hello?" — the model re-ran the lookup the silently-failed turn never did. **Model lineup settled by measurement, then re-settled by a full catalog sweep (2026-08-05):** the configured lineup was not the available one — six tool-capable free models had been released since it was written, and the incumbent was the oldest of the thirteen. Swept all of them (one cheap probe each, then the full golden set on the survivors, with turn latency now measured): `poolside/laguna-s-2.1:free` **10/10 @ 10.8s** is primary, with `inclusionai/ling-3.0-flash:free` (9/10) and `nvidia/nemotron-3-ultra-550b-a55b:free` (9/10) behind it; shipped measures **8/10 @ 9.0s median**, up from 5/10. Dropped: gpt-oss-20b (5/10), nemotron-3-super (3/10), gemma-4-31b (0/10 — pool refuses us; and that is also why it never appeared in the OpenRouter dashboard, since rejected requests never become generations). Avoid `nvidia/nemotron-nano-12b-v2-vl:free`, which hung 90s on a one-line probe. **Free-model robustness added the same day:** per-step and per-chunk timeouts (`stepMs` 20s, `chunkMs` 15s — the stall detector for a stream that opens and then stops) plus `maxOutputTokens`, so a slow or rambling free model degrades into a visible failure rather than a hang. **The "no tool call at all" open item was largely the model** and does not reproduce on laguna-s. **Also fixed here (2026-08-04):** OpenRouter free-tier rate limiting, which a first pass wrongly wrote off as an upstream problem. The cap is 20 requests/minute *across the whole account*, so `FALLBACK_MODEL_IDS` (all `:free`) can never rescue a 429; we sat on the cap and the SDK's blind retries kept us there, while `Retry-After` was ignored. `src/agent/openRouterRateLimit.ts` now paces every free-tier request through one process-wide window, waits exactly as long as OpenRouter asks, never retries a per-day limit, and refuses to sleep past the caller's deadline; the chat API answers `429` + `Retry-After` instead of a 500. | `PWA_UX_FIX_PLAN_2026-08-04.md` |
| 10 | **PWA UX round 5 — four items from real phone use (2026-08-05).** *(a)* **Map gestures.** The map container carried `touch-none`, so it owned every touch and a drag starting on it could never scroll the sheet past it. Now `cooperativeGestures`: one finger belongs to the sheet, two pan/zoom the map, and fullscreen turns it off again because nothing sits behind the map to scroll. *(b)* **Fullscreen map**, opened by tapping the map or the top-right button, which inverts to a collapse icon (video-player convention). Deliberately a portalled fixed overlay, not the Fullscreen API — iOS Safari does not support `requestFullscreen` on arbitrary elements. **Two defects the DOM assertions passed and only the screenshot caught:** `fixed inset-0` resolves against the nearest *transformed* ancestor, and vaul's drawer content is transformed, so "fullscreen" covered only the drawer until it was portalled to `<body>`; and portalling gives the container a new node, so the map must be re-created — which does not re-run the marker effect, and fullscreen opened with all five pins missing until `fullscreen` joined its deps. *(c)* **Satellite had no labels at all** — the style was a lone swisstopo raster — so a CARTO labels-only raster is composited on top. `dark_only_labels` is the variant drawn *for* dark basemaps and renders light text with a dark halo; `light_only_labels` is the inverse and disappears over aerial. *(d)* **Loading trace banding.** The five nominal opacities were already compositing-corrected to an even 0.15/0.35/0.60/0.82/1.0, so re-balancing five layers could not have helped: the cause was the step *count*, ~0.2 of accumulated alpha across each ~4px boundary. Now nine layers with the ramp solved backwards from a linear target Aₖ=k/9, giving the series 1/9, 1/8 … 1/2, 1, and dashes right-aligned so all nine accumulate at the leading edge. **Still open from this round:** the reported map/list availability contradiction is *unconfirmed* — both read the same `store.available` on the same filtered array, and the likeliest cause was the gold user-location dot painting over the one green marker (it is larger, and the user stands at that store), so store markers now sit above it; a live repro with a store the user is standing in has not been done. | — |
| ~~11~~ | **Fixed and gated 2026-08-06 — recall 0.768 → 0.871, and the gate can now see it.** The "unresolved design problem" below was a false dilemma, and antigravity named the way out: fixture blindness was a property of *when we captured*, not of fixtures. `eval:capture -- --pool` now snapshots the candidate pool **before** `productMatches` runs (`src/eval/fixtures-pool/`, 51 queries), so recall is measured offline, deterministically, with no live vendor call in CI — `poolRecall.test.ts` ratchets mean recall plus a per-query floor, and hard-fails any query whose relevant products are *all* discarded. **The defect was German morphology, twice.** (a) `TAXONOMY` was keyed by English concept names (`fruit -> ['obst', …]`) and looked up by the shopper's German token, so `obst` and `teigwaren` expanded to nothing — and since an unrecognised token is mandatory, every product that did not spell the category word out was discarded: "Teigwaren" kept 10 of 87 relevant products, "Reinigungsmittel" 0 of 32. Re-keyed German-first and **directional** (a hypernym expands to its members; a member never expands to siblings, or "Apfel" would answer with a banana). (b) A closed compound never matched the open form the retailer shelves it under — "Freilandeier" vs "Eier, Freiland", "Basmatireis" vs "Basmati Reis". Split matching requires **both** halves on the same product, so it is strictly harder to earn than the whole token. **Precision rose in the same change** (pool P@5 0.941 → 0.969), because for a category query the old filter kept only the junk that spelled the word out: the top four results for "Obst" were baby purée, vinegar, mixed fruit and a Dr. Beckmann *stain remover*. Fixed by scoring the **head** of a German compound — `Mischobst` is fruit, `Obstessig` is a vinegar — in the matcher and, separately, in the eval's judge. **The judge was the more serious finding:** it matched labels as bare substrings, so it certified that stain remover as *relevant* and scored "Obst" P@5 1.0. The published 0.98 was never real; the honest number with the ranker untouched is 0.941, and the matcher work then took it to **0.973**. `forbidden` deliberately still matches anywhere — "Erdnussbutter" is forbidden for "Butter" precisely through its modifier. **Still open, deliberately:** the stain remover is still #3 for "Obst" because "Obst" is a standalone word in its name and this judge sees identity only — its category says `Fleckenentferner`, so a category-contradiction signal is the next lever. Several floors in `poolBaseline.json` are low because the **labels** are generous (`ruebli` counts Purina cat food with carrots as relevant, `freilandeier` counts any plain egg); those are label defects in `goldenSet.ts`, and raising the matcher to chase them would be optimising to a bad label. Rejected on antigravity's advice: moving the filter post-merge (adapters slice to `limit` *after* filtering, so it would truncate before relevance is judged) and pool-derived hypernym rescue (circular — Otto's carpet pool would teach `curry`↔furniture — and it would make Migros recall depend on Coop being up). | `src/eval/poolRecall.ts`, `poolBaseline.json` |
| ~~11-orig~~ | ~~**Recall is the real relevance problem, and the gate is blind to it (measured 2026-08-05).**~~ `scripts/measureFilterLoss.mjs` runs the whole golden set against the live fan-out and tallies, per chain, what the vendors returned versus what `productMatches` then discarded. First run: **vendors returned 4079 products, we rejected 1853 — 45.4%**, with **0/51 queries starved by a vendor** and 0/51 where we discarded everything. Per chain: ottos 70%, lidl 66%, migros 49%, aldi 44%, coop 28%, volg 0%. Filtering loosely-related products is the job, so 45% is not damning alone — the *shape* of the worst cases is: `reinigungsmittel` 82→2, `aproz` 41→1, `roter thai curry` 60→2, `teigwaren` 134→11, `obst` 82→12. Category and brand words, failing for the same reason "Milchdrink UHT" did (item 2): an unrecognised token is mandatory, and an apple is named "Gala Äpfel", never "Obst". **The golden set scores P@5 0.98 on this exact data** — precision is fine, the survivors are relevant; it has never measured recall. **Design problem, unresolved — do not code around it:** a recall metric over the *fixtures* cannot see this either, because fixtures are captured post-filter and the rejected products are already gone from the snapshot. It would be a second metric blind to the same defect. The measurement must run against the live pre-filter pool, so the likely answer is to promote filter-loss to a gated metric with per-query floors rather than bolt "recall" onto the fixture eval — **consult antigravity before committing to that.** *Consequence for planning: this supersedes the web-search work.* Items 8's provider question, sitemap ingestion and the Firecrawl/Tavily tiers are all downstream of a problem that is not the binding one — no web search buys back products we already had and threw away. **Own-inventory index (owner's proposal, 2026-08-05):** right long-term shape — local FTS5 over an enumerated catalogue kills the 19s search, the rate limits and the anti-bot surface permanently, and `searchService.ts:380` already upserts vendor results into that catalog. But **sequence it after the matcher**: an index does not fix the filter, so building it first just re-houses the same 45% loss. | `scripts/measureFilterLoss.mjs` |
| 3 | **Phase 7 — multi-conversation chat.** IndexedDB v2 + migration + history sheet. Chat history is currently one fixed record. | `PWA_UX_FIX_PLAN_2026-07-30.md` §2.11 |
| 4 | **Phase 4 carve-out: real `AbortController` cancellation.** Deliberately skipped when Phase 4 landed. Needs an interface change on `ChainAdapter.searchProducts` threaded through all seven adapters plus the Playwright path — a refactor, not a perf fix. Leak is bounded today: `SourceHttpClient` gives every HTTP request its own controller/timeout, so only the remainder of a multi-step adapter survives a soft timeout. | tracker row "Phase 4 — fan-out performance" |
| ~~5~~ | ~~**Pi deployment — awaiting owner approval.**~~ **Approved and automated 2026-08-04.** Deploying was `workflow_dispatch`-only, so main could sit undeployed indefinitely with no outside signal about what the Pi was running. Every green CI run on `main` now deploys itself (`workflow_run`, gated on `conclusion == 'success'`, checking out the SHA CI actually tested). Manual dispatch is kept for rollbacks to a known-good SHA. The Pi re-runs lint/test/build itself before swapping the container — arm64, where CI is x64 — and `deploy-healthcheck.sh` rolls back to `:pi-slim-previous` on a failed health check. First automatic deploy: `4eecad2`, 2026-08-04, healthy on the second probe (HTTP 200 from `/api/source-status`). **Consequence worth knowing:** CI's default suite drives live vendor APIs, so a vendor outage can now block shipping — see the tolerance note in `mcp.prodpatterns.test.ts`. | `.github/workflows/deploy.yml` |
| ~~8~~ | **Measured 2026-08-06: production's web tier is alive, and the answer cost one HTTP GET.** `GET /api/metrics` → `webSearch` on the Pi, read over Tailscale once it came back up: **58 total searches, `ddg` 43, `ddgChallenges` 0, `circuitBreakerOpens` 0, no ddg entry in `fallbacksByProvider`.** The counters persist (`metrics-snapshot.json` in the `cache:/data` volume) so they are cumulative across restarts, which is why the 20 serpapi/hasdata/searlo requests are still visible — those are the pre-2026-07-25 paid chain, and the 43 ddg requests are everything since it was promoted to primary. So DDG has served 43 real production searches from behind Gluetun without being challenged once. The 2026-07-25 reading was not stale; it has been continuously true. **The lesson is the cheapness, not the result** — this row spent two revisions arguing about how bad an unmeasured capability was, when the telemetry to answer it was already deployed and one request away. *Caveat worth fixing separately:* the snapshot reports `providerFallbacks: 0` while `fallbacksByProvider` totals 5, so the aggregate counter is not wired to the same path as the per-provider one. **Still open, and unchanged:** the capture workflow's egress mismatch (below) — that finding was about test fixtures and stands on its own. Committing web fixtures is now optional rather than blocked. Original row, kept for the reasoning: **Production's web tier was measured once, on 2026-07-25, and never since — and the run we had queued would measure the wrong machine.** *(Correction, same evening: an earlier version of this row said production had **never** been measured. That was wrong, and the record was in the repo the whole time. `PWA_REALDEVICE_FEEDBACK_2026-07-25.md` item 23 documents a real production test through the Pi's actual Gluetun/ProtonVPN egress — `docker exec` over `tailscale ssh` — returning **HTTP 200, 1996 ms, 12 results, no challenge**. Done from inside the container, which is exactly the right place. The code comment on the auto chain's primary entry, "keyless, verified working through the deployed egress", is therefore accurate and was not the overreach it looked like.)* **What is actually true:** that green is 12 days old; it predates DDG's posture toward our dev egress hardening into a 202 challenge; and nothing has re-checked it. Note also what item 23 recorded about *when* it was taken — DDG had served **0 of the 12** real searches to that point, because the paid chain absorbed them all. DDG was promoted to primary in that same change, so unlike then, production's real traffic has been exercising it ever since. **That means the cheap measurement is passive and already built:** `GET /api/metrics` → `webSearch` already carries per-provider attempt/success/fallback/breaker counters from real traffic, which is precisely the telemetry antigravity recommended over a synthetic monitor — and it is the same endpoint item 23 read to discover the 0-of-12. Read it before probing anything. **Blocked right now:** `rpi-dev` is offline in Tailscale (last seen ~2026-08-05 evening), and the app binds `127.0.0.1:3000` on the Pi, so there is no route to either the metrics endpoint or a container probe until it is back. **Do not "fix" this by leaving the VPN** — Gluetun is load-bearing for Migros, whose abuse detection blocks the Pi's bare egress IP (see the self-hosted CI/CD row). **Google CSE tail:** `buildAutoComposite` appends it *only if* `GOOGLE_CSE_API_KEY` + `GOOGLE_CSE_CX`/`_ID` are set, so whether production even has a dead third hop depends on the Pi's `.env`, which is Pi-only and unread. Pruning it was queued as unconditional; it is now waiting on that check, because making a code change on an unverified premise is the exact failure this row exists to record. **The capture-workflow finding below stands unchanged, and is the part that was right.** The capture mechanism is built and committed (`npm run eval:capture -- --web`, `WEB_TIER_QUERY_IDS`, `augmented` stamping, suite fails on a non-augmented fixture) but **no web fixtures are committed**: from the dev machine both DuckDuckGo endpoints return an HTTP 202 bot challenge and google CSE returns a permanent 400, so a capture produced pools identical to the vendor tier for 49/51 queries. That is coverage in name only, so it was discarded rather than committed. It was then written up as unblocked-and-trivial — "a single `npm run eval:capture -- --web` on the Pi plus a commit of the diff". **Re-examined 2026-08-06; that framing was wrong twice over.** *(1) The queued run measures a different egress than production uses.* `eval-capture-web.yml` is `runs-on: [self-hosted, linux, arm64, pi]`, so it executes on the Pi **host**. Production is the app container, which runs `network_mode: "service:gluetun"` (`deploy/docker-compose.pi.yml:25`) — every request it makes leaves through a commercial VPN exit, never the Pi's residential IP. The workflow's own comment asserted "the Pi is a residential egress DDG does serve"; true of the runner, false of the app, and DDG challenges VPN/datacenter ranges harder than residential ones. **So the capture is systematically more likely to pass than production is**, and a green run would have been committed as evidence about a capability it never touched. Two questions had been collapsed into one: *can we capture fixtures from somewhere* (the workflow answers this, legitimately, about test data) versus *does web augmentation work in production* (nothing answers this, and running the workflow would not). Comment corrected in place. Confirmed with antigravity, which called the split right on both the Docker networking semantics and DDG's IP-reputation behaviour. *(2) The chain is one vendor deep, and that is the standing risk whatever the last reading said.* `SWISS_SHOPPING_WEB_SEARCH=auto` is the Pi's default; the chain is ddg-html → ddg-lite → (google-cse only if keys are set), and hops 1 and 2 are the same index behind the same IP reputation. Google CSE has been closed to new customers since 2026-07-13 and answers our dev egress with a permanent 400, so the tail cannot be counted on to catch a DDG outage. **Nothing in the suite can tell us if that happens:** with `fixtures-web/` empty, `presentTiers` drops the web tier from the scored set entirely and "only counts web-tier fixtures where augmentation actually contributed" passes over an empty array — vacuously green, by design and honestly documented, but green all the same. **How bad, precisely.** Runtime cost is bounded — 8s soft timeout (`searchService.ts:37`) plus per-provider circuit breakers — and since `662ff31` a dead chain reports itself as a failed source instead of an empty web, so this degrades rather than breaks. The damage is that a 12-day-old reading is being carried as current on a path whose upstream is actively hostile and demonstrably moved against us on the other machine in that window. **And its low rank expired on 2026-08-06.** This sat last because item 11 was the binding constraint — "no web search buys back products we already had and threw away". Item 11 closed today. Nobody re-ranked this; its position is stale, not judged. **Next move, in order, once `rpi-dev` is back on Tailscale.** (a) Read `GET /api/metrics` → `webSearch` first. It is free, it reflects real traffic rather than a synthetic request, and it answers the question directly: DDG attempts with successes means the tier is alive; attempts with only failures or an open breaker means it died since 2026-07-25. (b) Only if the counters are empty or ambiguous, re-run the item-23 probe from inside the namespace — `docker compose cp scripts/probeWebSearch.mjs swiss-shopping:/app/scripts/` (the image ships `dist/` and `node_modules/` but not `scripts/`, and the script's `../dist/sources/webSearch.js` import needs exactly that path), then `docker compose exec swiss-shopping node scripts/probeWebSearch.mjs`. (c) Check whether the Pi's `.env` still carries `GOOGLE_CSE_API_KEY`/`_CX`; prune the tail from `buildAutoComposite` if it does, since it is a hop that can only spend latency to fail. (d) Only then decide about fixtures. **Rejected on antigravity's advice:** a synthetic monitor that repeatedly probes DDG over the VPN — added automated velocity from a flagged range is the behaviour being detected, so the monitor would help cause the outage it watches for. Surface the per-provider breaker/telemetry state production already collects from real traffic instead. If the chain is to survive at all it needs an authenticated provider promoted into `auto` (serpapi/hasdata/searlo/firecrawl exist as classes and are deliberately excluded today), not a better scraper. | `src/eval/README.md`, `.github/workflows/eval-capture-web.yml`, `deploy/docker-compose.pi.yml` |
| 12 | **The Pi came back from a power cut with nothing working, and nothing said so (2026-08-06).** A mains cut stopped it at 18:03 on 2026-08-05; it stayed down 17h42m until powered on by hand. A deploy queued at 23:23 UTC sat there the whole time. **A queued job on a dead self-hosted runner produces no failure, no email and no red** — the only outside signal is "the deploy is slow", which is indistinguishable from a slow build. Three separate faults, and only one of them was the power. **(a) The Pi does not power itself back on.** That is the Argon ONE V2 case, whose MCU holds the board off until the button is pressed; the fix is a physical jumper to pins **2+3** ("Always ON") and there is no software substitute. **Owner action — the case has to be opened.** The usual side effect does not apply here: the "fan pinned at 100% in Always ON" reports all trace to I2C being off so `argononed` cannot drive the fan, and this Pi has `dtparam=i2c_arm=on`, `/dev/i2c-1` present, `argononed.service` active and the MCU answering at `0x1a`. **(b) The runner was dead even with the Pi up, and that is the worse one.** The Pi 4 has no RTC, so `fake-hwclock` restores the last saved time at boot — measured **17.7h in the past**. The runner starts at that stale time, completes its TLS handshake (`√ Connected to GitHub`), fails session creation, and reports **"The runner registration has been deleted from the server, please re-configure"**, which is simply untrue: `GET /actions/runners` still listed it throughout. It then logs "no retry needed" and exits **`status=0/SUCCESS`**, so `Restart=on-failure` can never catch it and the service stays dead until a human intervenes. Proved the clock was the cause rather than the registration by restarting once NTP had synced: connected in 3s and immediately picked up the 10-hour-old queued job. Fixed with a drop-in on the Pi, `…rpi-dev.service.d/10-wait-for-clock.conf`: an `ExecStartPre` that blocks until `timedatectl` reports `NTPSynchronized`, bounded at 300s so the unit cannot sit in `activating` forever, plus `Restart=always`/`RestartSec=15` and `StartLimitIntervalSec=0`. **Deliberately not `systemd-time-wait-sync.service`** (antigravity's warning, and it is right): that gates the global `time-sync.target` and would stall boot for everything else whenever the Pi comes up before the router — which after a power cut is the normal case. This blocks only the runner. **(c) Nothing watched any of it.** `pi-runner-watch.yml` now does, hourly. **Two checks, because the obvious one is not available:** listing runners needs `administration: read`, which **is not a grantable key in a workflow `permissions:` block** — GitHub rejects the file at parse time — and falling back to `actions: read` returns **403 "Resource not accessible by integration"**. Both verified by dispatch, not read off the docs. So the default check is instead *any run still `queued` past 45 minutes*, which needs only `actions: read`, requires no new credential, and detects the exact damage that occurred. Its blind spot — Pi down with nobody pushing — is covered by the runner check, which stays in the file gated on a `RUNNER_STATUS_TOKEN` secret. **That token now exists** — fine-grained PAT `swiss-shopping-watcher`, this repo only, Administration + Metadata read-only, added 2026-08-06 and verified live (`pi runner status: online`), **expiring 2026-11-04**. When it lapses the workflow does not go red about a credential; it falls back to the queue check and opens one issue under its own title, because the real risk of a 90-day token is not the red run but monitoring silently halving itself and nobody noticing. Alerting is **edge-triggered**: one issue per outage, deduped while open, closed with a comment on recovery, so an outage costs one notification rather than one an hour. All three edges fired on purpose via the `simulate` dispatch input and verified — issue opened + run red, second alert green with no duplicate, recovery green with the issue closed. **Departed from antigravity here:** it argued for dropping the workflow in favour of a `healthchecks.io` dead-man's-switch pinged by Pi cron. Its spam objection is answered by the edge-triggering and its cron-slip objection is real but irrelevant against a 17-hour fault; not taken because it adds an external account and a second place to look, and because a heartbeat *from* the Pi cannot report fault (b), where the Pi is up and only the runner is gone. **Also worth knowing:** a Pi boot with a stale clock makes `docker ps` lie — the containers read `Up 18 hours` three minutes after boot, because `StartedAt` was recorded pre-NTP and the clock then jumped forward. They had in fact restarted correctly (`restart: unless-stopped`); the elapsed time is the artifact. | `.github/workflows/pi-runner-watch.yml`, Pi: `10-wait-for-clock.conf` |
| 6 | **Aldi "Protein Milch" returns bread.** *(2026-08-01: does not currently reproduce — Aldi now returns **zero** results for this query, so the bread is gone but so is the milk. `matcher.ts` last changed 2026-06-19, six weeks before the report, so nothing in the relevance logic fixed it; treat it as masked by vendor churn or the per-chain timeout, not repaired. Armed as a gate query in `src/eval/`.)* Original note: Real product lives on the unbuilt `aldi-now.ch` storefront; plus an ingredient-text AND-match false positive in `matcher.ts`/catalog FTS5. Overlaps item 2. Deferred by owner 2026-07-30. | — |
| ~~7~~ | ~~**Two pre-existing live-network test flakes.**~~ **Fixed 2026-08-01.** Both files now call `vi.setConfig({ testTimeout: 30_000, hookTimeout: 30_000 })` at the top, scoped to the two files that drive real vendor endpoints rather than raising the global default and blinding the unit suite. 194/194 pass. The flakes were correctly diagnosed earlier but left red across several sessions — a known-failing suite trains everyone to ignore red, so a verified-pre-existing failure still needs fixing, not just labelling. | — |

Three defects the golden set could not have caught, found by driving the SPA in
a browser on 2026-08-01 while verifying item 2, and fixed with it:

- **The eval and the browser disagreed.** Catalog hits were merged into results
  without any relevance check — the comment claimed "apply the same filters as
  vendor search" and only applied `maxPrice` — so "glutenfreies Brot" returned
  Butterzopf in the UI while the fixtures looked clean, because
  `eval:capture` runs without a local catalog. **Any future relevance claim
  measured only by `src/eval/` inherits that blind spot.**
- **Constraints leaked through the co-occurrence taxonomy.** One "Glutenfreies
  Brot" in a result set teaches `buildTaxonomy` that `glutenfreies` goes with
  `brot`, after which every bread scored 42. Diet/allergen claims now refuse
  the taxonomy path entirely.
- **Two SPA render crashes.** A source warning with no chain (the aggregated
  web-search failure carries none) threw on `undefined.charAt` and replaced the
  whole result list with "Network error"; and Otto's returned the raw OCC
  opening-hours *object* where `NormalizedStore.openingHours` is typed
  `string`, which the response cast hid from the compiler and which broke the
  store list the same way.

Round-3 PWA UX fix plan status: **Phases 1–5 done** (1 on 2026-07-30; 2, 3, 4, 5
on 2026-07-31), Phases 6–7 open. Full plan with root causes and rejected
alternatives: `docs/active/PWA_UX_FIX_PLAN_2026-07-30.md`.

Note on Phase 5: the plan's stated cause ("widen the GeoAdmin origins and
Sihlcity resolves") turned out to be wrong on the facts — see the Phase 5 row in
the status table for what was actually true. Treat the remaining phases'
diagnoses as hypotheses to verify against the live API, not as settled fact.

## Current phase

Phase: `V1 - core read/search foundation`

## Goals (V1)

- Define stable normalized domain contracts
- Implement chain adapter skeletons
- Implement searchable product/store tools
- Add automated tests and CI-ready quality gates

## Status

| Area                                | Status                 | Notes                                                                                                                                                                                                                                                                                                                                                                                                               |
| ----------------------------------- | ---------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| TypeScript scaffold                 | done                   | Base project and build scripts exist                                                                                                                                                                                                                                                                                                                                                                                |
| MCP bootstrap                       | done                   | `src/index.ts` server skeleton is running                                                                                                                                                                                                                                                                                                                                                                           |
| Domain types                        | done                   | `src/adapters/types.ts` established                                                                                                                                                                                                                                                                                                                                                                                 |
| Adapter implementations             | done                   | Static multi-chain adapters for all V1 chains; HTTP `MigrosAdapter` in `src/adapters/migros.ts` for future live integration                                                                                                                                                                                                                                                                                         |
| Tool implementations                | done                   | `search_products`, `find_stores`, `compare_prices`, `get_store_availability_support`, `lookup_store_product_availability` wired via `src/tools/handlers.ts`                                                                                                                                                                                                                                                         |
| Test baseline                       | done                   | Adapter/service/tool-handler/integration tests; HTTP adapter tests in `migros.test.ts`                                                                                                                                                                                                                                                                                                                              |
| HTTP utility                        | done                   | `src/util/http.ts` — `fetchJson` + `HttpError` for use by HTTP adapters                                                                                                                                                                                                                                                                                                                                             |
| Price comparison service            | done                   | `src/services/priceComparisonService.ts` — cross-chain unit-price comparison                                                                                                                                                                                                                                                                                                                                        |
| Search service                      | done                   | `src/services/searchService.ts` — multi-chain fan-out with filtering                                                                                                                                                                                                                                                                                                                                                |
| Store availability lookup           | done                   | `get_store_availability_support` and `lookup_store_product_availability`; static Migros adapter supports per-store availability                                                                                                                                                                                                                                                                                     |
| Search usability improvements       | done                   | `matchMode` balanced/literal matching added; pasta-family recall fixed for static catalog                                                                                                                                                                                                                                                                                                                           |
| Unit-aware price comparison         | done                   | `comparisonBasis` pack/unit ranking, normalized unit eligibility, and `limitPerChain` alternatives implemented                                                                                                                                                                                                                                                                                                      |
| Real data migration plan            | implementation-started | `docs/active/REAL_DATA_IMPLEMENTATION_PLAN.md` drafted and Gemini-reviewed with GREEN LIGHT on 2026-05-18; Phase 0/1 slice underway                                                                                                                                                                                                                                                                                 |
| Chain source audit                  | in-progress            | `docs/active/SOURCE_AUDIT.md` created with initial audit for all eight chains; Aldi is the recommended first catalog candidate, Denner a promotions candidate, Farmy blocked                                                                                                                                                                                                                                        |
| Aldi fixture-backed parser/adapter  | done                   | Product sitemap/product-page parsers, small real-source fixtures, fixture-backed product adapter, and `docs/active/ALDI_FIXTURE_ADAPTER.md` added; `npm run lint`, `npm test -- --run`, `npm run build`, and `npm run test:live` pass; Gemini review found no blocking findings                                                                                                                                     |
| Aldi live-beta adapter              | done                   | Runtime Aldi product search now uses the live-beta adapter path with source text fetching, file TTL cache, provenance, stale-cache warnings, deterministic legacy-static test mode, unit tests, opt-in live smoke coverage, and `docs/active/ALDI_LIVE_BETA_ADAPTER.md`; `npm run lint`, `npm test -- --run`, `npm run build`, `npm run test:live`, and `LIVE_SOURCE_TESTS=1 npm run test:live` pass                |
| Denner promotions engine            | done                   | `docs/active/DENNER_PROMOTIONS_IMPLEMENTATION_PLAN.md` drafted and Gemini-reviewed on 2026-05-19; Denner fixture-backed parser, live-beta promotions adapter, `search_promotions` tool, and opt-in `compare_prices.includePromotions` effective-price ranking added; post-implementation Gemini blocker fixed and follow-up review found no blockers; `npm run lint`, `npm test -- --run`, and `npm run build` pass |
| Real data infrastructure            | done                   | Provenance/source-warning domain types, file TTL cache, source HTTP client, source health service, partial-warning propagation, and validated opt-in `test:live` script added; `npm run lint`, `npm test -- --run`, and `npm run build` pass                                                                                                                                                                        |
| Real data infrastructure test cases | done                   | Additional cache/client/service/tool metadata cases added and documented in `docs/active/REAL_DATA_TEST_CASES.md`; `npm run lint`, `npm test -- --run`, `npm run build`, and `npm run test:live` pass; Gemini review found no blocking findings                                                                                                                                                                     |
| Production readiness audit          | done                   | `docs/active/PRODUCTION_READINESS_AUDIT.md` added on 2026-06-15 after repo audit and Gemini second-pass review requested with `gemini-3.1-pro-preview`; conclusion: default runtime is not production-ready while static catalog data can power product/store/availability responses, and live multi-chain product search needs a realistic source/index/provider architecture before production claims |
| Production readiness plan           | reviewed               | `docs/active/PRODUCTION_READINESS_IMPLEMENTATION_PLAN.md` drafted on 2026-06-15 with web research plus Gemini planning/review input; plan prioritizes removing static runtime/fallback data, adding source status, stabilizing live source-backed capabilities, deciding provider/index architecture for product search, and adding latency/telemetry/circuit-breaker safeguards |
| Source provider decision record     | done                   | `docs/active/SOURCE_PROVIDER_DECISION_RECORD.md` compares official APIs, paid providers, maintained index, and local crawling; local crawling rejected for broad product search |
| Source registry and get_source_status | done                 | `src/adapters/sourceRegistry.ts` with 8-chain × 5-capability status matrix; `get_source_status` MCP tool added; 117 tests pass |
| README production alignment         | done                   | README rewritten with honest source support matrix and data trust policy; aspirational feature claims removed |
| CI workflow                         | done                   | `.github/workflows/ci.yml` added; lint + test + build gate on main and PRs |
| Production release checklist        | done                   | `docs/active/PRODUCTION_RELEASE_CHECKLIST.md` added |
| Store source interface              | done                   | `src/sources/storeSource.ts` with ObservedStoreRecord, Haversine distanceKm, filterByRadius, sortByDistance; geospatial fields added to StoreSearchFilters |
| Product provider abstraction        | done                   | `src/providers/productProvider.ts` with ProductProvider interface and SWISS_SHOPPING_PRODUCT_PROVIDER env-var resolution; default is 'none' (returns unsupported) |
| Copilot instruction architecture    | done                   | Root and `.github` instruction files realigned                                                                                                                                                                                                                                                                                                                                                                      |
| Live MCP manual test pass           | done                   | Original 31/31 baseline plus 6/6 follow-up regression cases pass; see `docs/active/MCP_TEST_REPORT.md` (2026-05-18)                                                                                                                                                                                                                                                                                                 |
| Production patterns MCP test suite  | done                   | 113 new end-to-end MCP tests via loopback transport covering all 7 tools, error handling, data contracts, cross-tool integration, edge cases, and performance; see `docs/active/MCP_PROD_PATTERNS_TEST_REPORT.md` (2026-06-16)                                                                                                                                                                                       |
| Vendor coverage implementation plan | reviewed               | `docs/active/VENDOR_COVERAGE_IMPLEMENTATION_PLAN.md` drafted with web research + Gemini 3.1 Pro review; covers Migros, Coop, Aldi, Denner, Lidl, Otto's, Volg with phased approach; Migros uses `migros-api-wrapper`, Coop needs DataDome investigation                                                                                                 |
| Vendor coverage full implementation | done                   | All 8 chains implemented with live-beta adapters: Migros (`migrosLiveAdapter`), Coop (`coopLiveAdapter`), Lidl (`lidlLiveAdapter`), Otto's (`ottosLiveAdapter`), Volg (`volgLiveAdapter`); shared geocoding utility (`src/util/geo.ts`); parsers for all chains; source registry updated to live-beta for 7/8 chains; 343 tests pass, lint+build clean |
| Production patterns MCP test suite  | done                   | 113 new end-to-end MCP tests via loopback transport covering all 7 tools, error handling, data contracts, cross-tool integration, edge cases, and performance; see `docs/active/MCP_PROD_PATTERNS_TEST_REPORT.md` (2026-06-16)                                                                                                                                                                                       |
| Taxonomy fix + expansion            | done                   | Fixed `brod` typo to `brot` in `matcher.ts` TAXONOMY; added 17 new Swiss/German grocery term categories (water, beer, wine, yogurt, butter, eggs, coffee, tea, sugar, salt, oil, meat, chicken, fish, fruit, vegetables, cleaning) |
| Denner validity marker fix          | done                   | Replaced fragile `bodyText.indexOf(cardText)` offset matching with DOM-order traversal: iterate all elements, track current "Bis" date, assign to `.product-item` cards in document order |
| Critical fixes: ESM, SSRF, cl unit | done                   | C1: tsconfig `module: node16` + `moduleResolution: node16` for proper ESM resolution; C2: SSRF URL validation in `SourceHttpClient` blocks private IPs/localhost/loopback; C3: `cl` (centiliter) unit handling in `denner.ts` normalizeUnit and `units.ts` getBaseUnitPrice |
| Real API URL fixes (Phase 1)       | done                   | All 5 broken adapter URLs fixed: Migros uses `migros-api-wrapper` (guest token auth + cache), Coop uses REST API with iOS Safari UA, Lidl uses Lidl Plus app API, Volg uses WooCommerce REST API, Otto's uses OCC v2 API; parsers updated; 418 tests pass |
| Real API URL fix plan + review     | done                   | `docs/active/REAL_API_URL_FIX_PLAN.md` drafted and Gemini-reviewed with GREEN; plan committed as `8de8595` |
| Contract tests + test scripts (Phase 2) | done              | 5 contract test files (`*.contract.test.ts`) for Migros/Coop/Lidl/Volg/Ottos; `test:integration` and `test:contract` npm scripts added; Denner live test skip pattern fixed; 418 tests + 10 skipped contract tests |
| SPA chain summary (Phase 3)        | done                   | SPA shows "X of Y chains responded" with failed chain names when some adapters fail; Product Search, Store Finder, and Price Comparison tabs updated |
| Adapter/parser real-API fixes      | done                   | Migros rewritten (two-step search→details, `offer.price.effectiveValue`); Coop rewritten (path-param search, `resolveLocation()`, real field names); Lidl parser fixed (groups, array stores, storeKey); Otto's store URL + HTML stripping; all mocked unit tests updated; 18 test failures fixed; committed as `ebbcb97` |
| GeoAdmin geocoding replacement     | done                   | Replaced static 270-entry ZIP database with async `resolveLocationAsync()` calling Swiss GeoAdmin SearchServer API; in-memory cache, 3s timeout, static DB fallback; Coop/Migros adapters updated; 6 new async tests with mocked fetch; 424 tests pass, build clean |
| Product URL linking                | done                   | Added `productUrl` to `NormalizedProduct` type; propagated from Migros API (`url`), Coop API (`url`), Otto's (constructed from `code`), Volg (WooCommerce `permalink`), Aldi (existing `sourceUrl`); SPA product cards now clickable links to vendor pages; 424 tests pass, build clean |
| Adapter bug fixes                  | done                   | Migros product search: added fallback keys (`results`, `items`, nested search) for API response; Migros store search: bypassed wrapper to use `fetch` directly with lat/lon/radius; Coop store parser: handles `locations`, `stores`, `results`, `items`, `data` keys plus `lat`/`lng`/`lon` coordinate variants; Aldi parser: returns `undefined` for missing prices instead of throwing, defaults currency to `CHF`; 424 tests pass, build clean |
| Store finder fix + dynamic taxonomy + SPA URLs | done | Coop store search: separate `SourceHttpClient` with `iosSafariUA` to bypass DataDome; Migros store search: added `iosSafariUA` header + text-only fallback when coords fail; Dynamic taxonomy builder (`taxonomyBuilder.ts`): scans product names/brands/categories for token co-occurrence, Jaccard similarity, max 8 aliases; `matcher.ts`: all match functions accept optional `dynamicTaxonomy` override; `searchService.ts`: builds dynamic taxonomy from adapter results, re-sorts with it; SPA store rendering: grouped by chain with per-chain status badges (green/red dots); 433 tests pass, build clean |
| Fix round 2: auth, images, URLs, SPA UX | done | Migros store 403: changed auth header from `Bearer` to `leshopch` (3 fetch calls); Migros CHF 0.00: removed `\|\| 0` fallback + zero-price acceptance + min-price guard; Otto's images: prefix relative URLs with domain; Otto's product URL: use API-provided `url` field; SPA comparison: wrap product names in `<a>` tags; SPA Enter key: added keydown listeners; Maps: replaced OpenStreetMap with Google Maps; 433 tests pass, build clean |
| Denner product search + store search fixes + availability | done | Migros/Coop availability implemented via `/store-availability/public/v2/` and `/products/{id}/stockLevels`; Coop store fix: separate `SourceHttpClient` with `iosSafariUA` + `currentPage=0`; Lidl store fix: switched to `/api/v4/CH` + client-side filtering; Denner product search: Prediggo API (`POST /search-api/simplePageContent`); SPA availability tab added; 435 tests pass, build clean |
| Otto's image fix                     | done                   | Root cause: `www.ottos.ch/medias/...` returns SPA HTML; actual image server is `api.sherpaoutdoor.com` (SAP Hybris OCC backend, found via `<meta name="occ-backend-base-url">`). Changed `parsers/ottos.ts` image prefix to `https://api.sherpaoutdoor.com` |
| Otto's price fix                     | done                   | `parseFormattedPrice()` now strips Swiss apostrophe (`'`) thousands separator (`CHF 1'399.00`) |
| Migros store 403 fix                 | done                   | Removed direct `fetch()` bypass (TLS fingerprint mismatch); uses `migros-api-wrapper`'s `searchStores()` for all queries |
| SPA availability API + UI            | done                   | `POST /api/availability` endpoint; SPA Availability tab with chain/query/storeId form |
| SPA source status update             | done                   | Updated to reflect all current adapter statuses |
| Source registry accuracy fix         | done                   | Migros nutrition + availability changed to live-beta; Coop availability changed to live-beta |
| SPA availability redesign            | done                   | Replaced store-ID-required form with postal-code-based store finder UX; new `/api/store-availability` endpoint; in-stock-only and currently-open filters; Google Maps links; opening hours display; 435 tests pass |
| SPA product quantity display         | done                   | Product cards and price comparison table now show size/quantity; 435 tests pass |
| SPA nutrition display                | done                   | Expandable nutrition section on product cards (energy, protein, carbs, fat, fiber, sugar per 100g); "Show all nutrition" toggle in search form; only for Migros products with nutrition data; 435 tests pass |
| SPA ingredients display              | done                   | Expandable ingredients section on product cards; "Show all ingredients" toggle in search form; `ingredients` field added to NormalizedProduct type; 435 tests pass |
| Unsupported chain graceful degradation | done | All unsupported chain queries now return `ok:true` with empty data + source warnings instead of `ok:false` ALL_SOURCES_FAILED error; updated 15 unit tests |
| Non-blocking server scripts          | done                   | `start-server.ps1` uses `cmd /c` for fully detached process (fixes opencode terminal lock); `kill-server.ps1` updated with safe variable names |
| Manual SPA test suite                | done                   | `test-spa-manual.mjs` with 42 test cases covering all vendors, postal codes, edge cases; all pass |
| Round 2 issue-specific tests         | done                   | `test-issues-round2.mjs` with 33 tests across 13 categories; found and fixed: apfel taxonomy, test assertion mismatches; all pass |
| Browser-tested integration tests     | done                   | 13 vitest integration tests in `server.integration.test.ts` covering: city name vs postal code store search, XSS handling, quantity multiplier, openNow/inStockOnly filters, chain filtering, error handling; all 448 tests pass |
| Otto's store name fix                | done                   | Otto's API returns store IDs (e.g., "0259") as names; parser now uses `Otto's {town}` format when name is numeric; build clean |
| Migros hours format fix              | done                   | `isStoreOpen()` now handles Migros date+time format ("2026-06-19 08:00") by returning undefined (can't determine open status from single time); build clean |
| Migros store search auth fix         | done                   | Fixed `searchStores` calls to pass `{ leshopch: token }` instead of raw token string (wrapper expects object, not string); Migros store search now works |
| Availability APIs marked unsupported | done                  | Both Migros (`store-availability` returns 403) and Coop (`/stockLevels` endpoint removed) availability APIs confirmed broken; adapters marked `supported: false`; source registry updated; 4 tests updated |
| Coop nutrition extraction            | done                   | Added `fetchProductNutrition()` method to Coop adapter parsing nutrition from product detail page HTML; enrichment expanded from top 3 to top 5 products; source registry updated to live-beta; DataDome blocks detail page requests so nutrition is best-effort |
| SPA availability UX improvement      | done                   | Added warning banner when all chains report availability unsupported; changed "Unknown" to "N/A" for unsupported stores; improved in-stock count display; added note about API availability in tab description |
| Coop nutrition via product detail API  | done                   | Replaced broken HTML scraping (`dynamic-pageload` blocked by DataDome) with working REST product detail API (`/rest/v2/coopathome/products/{code}?fields=FULL`); returns `ingredients` string and `nutritionInformation` (including `nutritionInformationPerUnit.nutrients`); enrichment applied on both fresh fetch and cache hit paths; 474 tests pass, build clean |
| SPA JavaScript syntax fix            | done                   | Fixed TypeScript non-null assertion (`p.ingredients!.join`) in inline browser JS that broke all SPA interactivity |
| Product size field standardization   | done                   | Added `size` field to Migros parser (from `quantity` API field) and Coop parser (from `contentUnit`); both adapters now propagate `size` to `NormalizedProduct`; 474 tests pass |
| Source registry accuracy update       | done                   | Lidl productSearch/promotions marked unsupported with reasons; Coop nutrition description updated to reflect REST API usage; availability entries verified accurate |
| Browser MCP migration                | done                   | Replaced `browsermcp` references with `stealth-browser-mcp` in AGENTS.md; added vendor investigation instructions for Lidl, Migros, Coop |
| Coop availability API fix            | done                   | Replaced broken `/products/{id}/stockLevels` with working `/locations/searchAroundCoordinates?availabilityProductId={id}` endpoint; all Coop stores return real stock data (avail=true); Migros site in maintenance (all APIs 403 as of 2026-06-26); 474 tests pass, build clean; committed as `aa72ab1` |
| Lidl product search fix              | done                   | Root cause: Lidl.ch redirects to fr-CH based on browser locale; with de-CH in URL, search works (8 products for 'milch'). Replaced Lidl Plus API (campaign metadata only) with website HTML scraping; URL: `lidl.ch/q/de-CH/search?q={query}`; parser extracts from `data-gridbox-impression` attribute; source registry updated to live-beta; 477 tests pass; committed as `9dd8e26` |
| Migros Cloudflare bypass fix         | done                   | Root cause: Migros uses Cloudflare bot detection that blocks Node.js requests (returns fake maintenance page). Solution: Replaced migros-api-wrapper with direct axios calls using TLS 1.3 (minVersion: 'TLSv1.3') which bypasses detection. Guest token, search, and product details all work; availability API now also works via TLS 1.3 (GET /store-availability/public/v2/availabilities/products/{pid}?costCenterIds={storeId}). Some products return 404 (no data in system). 477 tests pass; committed as `9d15cb0` |
| Migros maintenance detection         | done                   | Added `isMaintenanceError()` to Migros adapter that detects Cloudflare maintenance pages (403/503 with `<title>maintenance</title>`). Returns clear error message "Migros is currently down for maintenance" instead of generic "Request failed with status code 403". Updated all catch blocks (search, stores, availability). |
| Lidl client-side rendering fix       | done                   | Root cause: Lidl.ch uses client-side rendering for search results; server-side `fetch` returns HTML without product data (products loaded via JS). Added Sec-Fetch-* headers to fetch call; parser returns empty array when no products found; adapter throws descriptive error. Updated source registry to `degraded` status. |
| Migros Playwright Cloudflare bypass  | done                   | Root cause: Cloudflare blocks all Node.js requests (axios, fetch, curl) to migros.ch — TLS 1.3 alone is insufficient. Solution: Added `playwright-core` dependency; new `migrosBrowser.ts` launches headless Chromium, navigates to migros.ch for Cloudflare clearance, then uses `page.evaluate(fetch)` from same origin to make API calls (guest token, search, product cards, stores, availability). All Migros endpoints now work end-to-end. Adapter rewritten to use browser module instead of axios. 476 tests pass (1 flaky timeout on live API test). |
| Auto-search on filter changes        | done                   | SPA Product Search: vendor checkbox, limit select, and max price input changes trigger search automatically (debounced 500ms for price); Availability tab: chain filter, limit, inStockOnly, openNow changes trigger auto-search |
| Coop product URL fix                 | done                   | Fixed relative URLs from Coop API (e.g., `/de/lebensmittel/.../p/4940251`) by prepending `https://www.coop.ch` in parser; "View on Coop" links now work |
| Coop product size fix                | done                   | Parser now combines `content` (quantity) + `contentUnit` (unit) fields from Coop API (e.g., "6l" instead of just "l"); added `content?: number` to CoopProduct interface |
| Migros availability stock levels     | done                   | Passes `stockCount` from Migros availability API through to SPA; displays "Few Available" (amber) for stock ≤ 3, "In Stock" (green) for stock > 3, "Out" (red) for stock = 0 |
| SPA search race condition fix        | done                   | Added AbortController to cancel stale search requests; added 300ms debounce on vendor checkbox auto-search; abort errors silently ignored |
| Lidl Playwright search fallback      | done                   | Created `lidlBrowser.ts` using Playwright to render Lidl.ch search pages (handles client-side rendering); adapter falls back to Playwright when HTML scraping returns no products; source registry updated to `live-beta`; 467 tests pass |
| Coop per-unit price fix              | done                   | Fixed `parseUnit()` in `coop.ts` to handle `content` as string or number (Coop API returns `"6"` not `6`); combines `content` + `contentUnit` to compute per-unit price (e.g., CHF 1.95/L for 6l milk); price comparison table now shows Coop per-unit prices; 477 tests pass |
| Migros ingredients HTML strip        | done                   | Migros MGB endpoint returns ingredients with HTML tags (e.g., `<strong>Vollmilch</strong>`); added `.replace(/<[^>]*>/g, '')` to strip tags before storing; SPA now shows clean ingredient text; 477 tests pass |
| Price comparison image column        | done                   | Added product image thumbnails to the price comparison table alongside chain badges; size, per-unit, and total columns added |
| Availability N/A cache fix           | done                   | Normalized cache keys in Migros/Coop adapters (removed limit from key) so inner availability lookups (limit:1) reuse outer search results (limit:5); eliminates redundant browser round-trips on first load |
| Migros availability store-finder fix | done                   | Added `userLatitude`/`userLongitude` to `StoreProductAvailabilityFilters`; geocodes user location once in `lookupAvailabilityByLocationProductsFirst` and passes coords to adapters; Migros adapter uses user coords instead of product query for `findStores` fallback |
| Coop sale detection                  | done                   | Mapped Coop API `hasPromotion`, `originalPrice`, and `listPromotions` fields to `price.original` and `promotionLabel` on `NormalizedProduct`; handles `listPromotions` as `string | string[]`; SPA shows strikethrough original price + green promotion label; 48 tests pass |
| Aldi special-buy tag extraction      | done                   | Parser extracts `¹`/`²` superscript markers from Aldi product page HTML (`base-price__superscript` class); adds `special-buy`/`limited-stock` tags to `NormalizedProduct`; SPA shows amber "Special Buy" or red "Limited Stock" badges |
| SPA promotion label display          | done                   | Product cards and availability tab now render `promotionLabel` as green text next to price; works for Migros (badge labels) and Coop (discount percentages like "40%") |
| `NormalizedProduct.promotionLabel`   | done                   | Added `promotionLabel?: string` field to `NormalizedProduct` type; propagated from Migros `promotionLabel` and Coop `listPromotions` |
| SPA loading indicator race fix       | done                   | Fixed AbortError in search catch block: early return prevents hiding loading spinner when a new search replaces the aborted one |
| Denner size parsing fix              | done                   | Fixed `cleanContentSize` to convert Prediggo `unit.g` (kilograms) to grams (e.g., `0.5 unit.g` → `500 g`); was incorrectly showing raw value as grams |
| Aldi Vue.js superscript fix          | done                   | Updated regex to handle Vue.js SSR comments (`<!---->`) between tag content and superscript character; now correctly captures `²` markers on products like BIO-Protein-Vollkornbrötli |
| Migros 404 availability graceful degradation | done | Stores returning 404 from Migros availability API now keep `supported: true` (instead of `false`) so UI shows stock status attempt rather than hard N/A |
| Denner promotion passthrough         | done                   | `promotionAsProduct` now passes `price.original` and `promotionLabel` from Denner promotion data; SPA shows strikethrough original price + discount label |
| SPA availability store filters       | done                   | "In Stock" and "Currently Open" checkboxes now filter displayed stores client-side; shows "(filtered)" in header and "No stores match filter criteria" empty state |
| Aldi availability investigation      | done                   | Investigated Aldi product pages: no per-store availability data available; only generic `schema.org/InStock` from JSON-LD; Aldi store finder is general location service not product-specific |
| Migros Glattzentrum costCenterId fix | done                   | `extractStoresFromResult` now uses `costCenterId ?? storeId ?? id` for Migros store IDs; MMM stores (like Glattzentrum) have different `costCenterId` vs `storeId`; availability API requires `costCenterId`; 404 errors keep `supported: true` instead of `false` |
| Migros open/closed detection fix     | done                   | `parseOpeningHours` now handles time-only strings (e.g., `"06:30"`) from Migros API in addition to datetime strings; `h.open.includes(' ') ? h.open.split(' ')[1] : h.open` extracts time component; stores now correctly show "Open" status |
| Aldi store availability API          | done                   | Implemented `findStores` (service-points API), `lookupStoreProductAvailability` (product-availability API), parsers for service-points and availability responses, Vue.js SSR comment regex fix; source registry updated to `live-beta`; API returns 403 from server (bot protection) |
| Denner amount parsing fix            | done                   | `cleanContentSize` now converts Prediggo `unit.g` (kilograms) to grams (e.g., `0.5 unit.g` → `500 g`); `unit.g` means kilograms in Prediggo API, not grams |
| Lidl promotions investigation        | done                   | Confirmed NOT possible: HTML scraping only returns single `price` per product; campaign API returns metadata only; no original/discount data from any Lidl source |
| Aldi product-page availability       | done                   | Confirmed NOT available from product pages; only generic `schema.org/InStock` from JSON-LD; no per-store stock data exists |
| Aldi availability checkbox default   | done                   | Aldi checkbox defaults to unchecked in availability tab since Aldi store availability API returns 403 (bot protection); Migros and Coop remain checked by default |
| Migros store type filtering          | done                   | `extractStoresFromResult` now filters by `storeType` to only include supermarket stores (`m`, `mm`, `mmm`); excludes Migrolino, PickMup, VOI convenience/pickup stores that don't have product availability |
| AGENTS.md: antigravity-mcp          | done                   | Replaced deprecated `gemini-cli` MCP with `antigravity-mcp` for AI-powered code review; added model priority order: gemini-3.1-pro-preview → gemini-3.5-flash-preview → gemini-3-pro → gemini-3-flash |
| Web-augmented semantic search       | done                   | `src/sources/webSearch.ts` (GoogleCustomSearchProvider + DuckDuckGoHtmlProvider with 202/anomaly challenge detection and request serialization), `src/services/webProductSearchService.ts` (site-restricted search → product-ID extraction → adapter hydration, per-query ID cache, never caches empty lists), `getProductsByIds` on Migros (product-cards) and Coop (detail API) adapters, hybrid merge in `SearchService` (web-ranked results lead, vendor dedupe keeps enrichment); env: `SWISS_SHOPPING_WEB_SEARCH=auto\|google\|ddg\|off`, `GOOGLE_CSE_API_KEY`, `GOOGLE_CSE_CX`; vitest env pins web search off; verified in browser: "toothpaste for sensitive teeth" 0→5 relevant results |
| Delivery model decision             | done                   | `docs/active/DELIVERY_MODEL_DECISION.md`: PWA-first on small VPS (Docker, Playwright-capable), MCP as secondary agent interface, optional Telegram bot Phase 3; native app and agent-only delivery rejected with reasons |
| Dockerization                       | done                   | Multi-stage `Dockerfile` on `mcr.microsoft.com/playwright:v1.61.1-noble` (tag pinned to `playwright-core` version; supports amd64+arm64), non-root `pwuser`, `SWISS_SHOPPING_CACHE_DIR=/data/cache` volume, healthcheck on `/api/source-status`; `docker-compose.yml` with cache volume + Google CSE env passthrough; `.dockerignore`; README Docker section. Verified on Docker Desktop: container serves `/`, `/app/`, `/api/reverse-geocode`; in-container Playwright Migros search returns live data; cache volume persists across container replacement; arm64 built via QEMU binfmt (`tonistiigi/binfmt --install arm64`) |
| PWA Phase 1                         | done                   | Mobile-first React PWA in `pwa/` (Vite + TS + Tailwind v4 + shadcn-style components + framer-motion + TanStack Query + vaul + vite-plugin-pwa), built to `dist/pwa`, served by the Node server at `/app` (old testing UI stays at `/`); availability-first landing, "Use my location" via new `/api/reverse-geocode` (static ZIP DB nearest-locality), chain/in-stock/open-now filters, animated card transitions with `MotionConfig reducedMotion="user"`, vaul product detail sheet, installable manifest + service worker (static shell only, `/api` never cached); icons generated by `scripts/generate-pwa-icons.mjs`; browser-verified at 390×844: geolocation -> "8001 Zürich", live Migros availability results, search grid + product sheet, SW registered at `/app/` scope; 513 tests pass |
| Lidl + Aldi web-search hydration    | done                   | `getProductsByIds` added to Lidl and Aldi adapters and registered in `DEFAULT_WEB_SEARCH_CHAIN_CONFIGS`. Aldi: slug (18-digit SKU suffix) -> product page fetch via cached `loadProduct`. Lidl: bare-ID search proved unsupported by lidl.ch, so hydration IDs are product page paths (`/p/de-CH/{slug}/p{id}`) fetched directly and parsed from server-rendered schema.org JSON-LD (`parseLidlProductPage`); capped at 3 IDs, 6h per-product cache; per-chain `maxProducts` added to web-search config. Live-verified both adapters and browser-verified web-augmented SPA search end-to-end (seeded query -> hydrated Lidl/Aldi cards); 511 tests pass |
| Farmy removal                       | done                   | Farmy (ceased operations) removed from `Chain` union, `ALL_CHAINS`, tool schemas, `sourceRegistry`, README matrix, SPA tests, and loop scripts; adapter factory now uses exhaustive per-chain mapping with no `UnsupportedChainAdapter` fallback; `createServer` accepts injected adapters; unsupported-chain MCP contract re-pinned via stub-adapter suite in `mcp.prodpatterns.test.ts` §17; README matrix synced to source registry; pre-existing lint errors in `lidlBrowser.ts`/`spaRegression.test.ts` fixed (lint now 0 errors); 501 tests pass; browser-verified Source Status shows 7 chains |
| Cache/TTL overhaul (Phase A)        | done                   | Part of the antigravity-reviewed 4-phase architecture overhaul (A cache, B provider resilience, C SQLite catalog, D provenance/observability). Tiered freshness policy `src/cache/freshnessPolicy.ts` (discovery 7d/30d/stale-90d, productMetadata 7d/30d, existence 1d/7d, normalPrice 1h/6h, promoPrice 1h/3h, availability 5m/15m, emptySearch 5m/15m); `FileTtlCache` soft/hard/stale record shape (`refreshAfter`/`expiresAt`/`staleUntil`; legacy records normalized to old behavior), `prune()` with stale deletion + LRU max-file eviction (`SWISS_SHOPPING_CACHE_MAX_FILES`, default 5000), hourly unref'd prune timer wired into `createDefaultAdapters` and web-search factory, `stats()`; discovery cache moved off hardcoded 24h to the discovery tier with stale-fallback when the provider fails; in-flight dedup (`provider+chain+normalizedQuery` promise map, key removed on settle); negative caching of confirmed-empty searches only — provider failures never cached as empty; 524 tests pass, lint 0 errors |
| Provider resilience (Phase B)      | done                   | Part 2 of 4-phase architecture overhaul. Deliverables: (1) `TypedWebSearchError` with retryable/non-retryable classification; (2) `CompositeWebSearchProvider` with failover (Google → DDG on retryable 429/quota/5xx/timeout; non-retryable 401/403/invalid-config surfaced immediately); (3) `SourceCircuitBreaker` integration on both providers (google: 3 failures → 5min cooldown; ddg: 3 failures → 10min cooldown); (4) `ProviderBudget` daily tracker with JSON persistence, `isExhausted`/`isLow` (<10%) checks — skip Google when budget exhausted/low; (5) Vendor strength evaluation in `SearchService.shouldRunWebSearch` — skip web discovery per-chain when ≥3 products with ≥80% price coverage (env-tunable); (6) Aggregated multi-domain search (single Google query across all configured sites, serial DDG fallback); (7) Stale-cache-before-failure in composite provider; 557 tests pass, lint 0 errors; new files: `src/cache/providerBudget.ts`, `src/sources/phaseB.test.ts`, `src/cache/providerBudget.test.ts` |
| SQLite catalog storage (Phase C)  | done                   | Part 3 of 4-phase architecture overhaul. `better-sqlite3` in-process SQLite catalog at `SWISS_SHOPPING_DB_PATH` (default `<cache>/catalog.sqlite3`). WAL mode + 5000ms busy timeout. Idempotent versioned migration runner with `schema_migrations` table. Schema: `products` (chain+product_id PK, status lifecycle with `consecutive_failures`/`gone_signals` thresholds, FTS5 virtual table with trigram tokenizer + triggers for sync), `product_observation` (append-only price history with index on chain+product_id+observed_at). Query normalizer: NFD + combining-char strip, unit canonicalization (kg→g, l→ml, cl→ml), German plural folding, synonym expansion from `product_synonyms` table. `CatalogService`: `upsertFromNormalizedProduct`, `recordHydrationFailure` (transient→consecutive+1→suspected_removed≥3; gone→gone_signals+1→removed≥3; reset on success), `search` (FTS5 + synonym expansion), `getStats`, `getSynonyms`. Wired into `SearchService` for catalog-augmented search (hydrate catalog-only hits via adapter). Dockerfile updated for native `better-sqlite3` compilation. 595 tests pass, lint 0 errors |
| Provenance + observability (Phase D) | done                 | Part 4 of 4-phase architecture overhaul. Provenance enrichment `src/catalog/provenance.ts`: every search result carries `_source`, `_discoveredBy` (vendor/web-google/web-ddg/catalog), `_observedAt`, `_priceObservedAt`, `_stale`, `_confidence` (heuristic: fresh vendor 1.0, web-discovered 0.8, stale 0.7, catalog-only 0.3). Observation validation `src/catalog/observationValidation.ts` applied in catalog upsert: normal-price drop >75% / rise >100%, zero/negative price, missing currency, promo>normal, package-size change, name mismatch → observation stored as `pending_verification` (migration 2 adds status column), credible value retained; two consecutive matching suspicious values accepted as new truth; `promotion_price` exempt from the drop rule (legit Swiss 50% promos). Metrics `src/util/metrics.ts`: cache hit classes, searches/query, provider budget + breaker events, hydration success and 404/410 rates, per-retailer latency, catalog coverage; JSON snapshots persisted to cache dir (periodic + on demand); exposed via `GET /api/metrics` and MCP tool `get_metrics`. No user-triggered force-refresh (rejected as quota-drain vector). 663 tests pass, lint 0 errors |
| Observation deduplication       | done                   | Short-window dedup in `CatalogService.upsertFromNormalizedProduct`: skips appending an observation when (chain, product_id) latest observation has identical price, promotion_price, currency, availability, size, and name within a configurable window (default 60 min, `SWISS_SHOPPING_OBSERVATION_DEDUPE_MINUTES` env override). `pending_verification` observations never suppress incoming observations. Product identity update (last_seen_at/last_verified_at, status reset) still runs on every upsert. 670 tests pass, lint 0 errors, build clean |
| Multi-provider web search       | done                   | Added SerpApiProvider, HasDataProvider, SearloProvider, FirecrawlSearchProvider to `src/sources/webSearch.ts`; `CompositeWebSearchProvider` refactored from primary/fallback to ordered chain with per-provider circuit breaker and budget; auto-mode chain: serpapi → hasdata → searlo → firecrawl → google (legacy) → ddg (emergency); `ProviderBudget` extended for all providers with per-provider daily budgets from env; metrics extended with per-provider request/fallback counters; `MetricsCollector` threaded from `index.ts` and `server.ts` through `createDefaultWebProductSearch` → `createWebSearchProviderFromEnv` → `CompositeWebSearchProvider`; `recordProviderRequest` before each attempt, `recordProviderFallbackFrom` on retryable failure, `recordCircuitBreakerOpen` on breaker skip; docker-compose, README updated; Google CSE closure documented; 95 tests pass, lint 0 errors |
| antigravity-mcp naming/model order sync | done              | Root `CLAUDE.md` was stale (still referenced deprecated `gemini-cli`, no model priority section) despite `AGENTS.md` already having migrated — synced both. `.github/mcp-config.json` key renamed `gemini-cli`→`antigravity-mcp` (command now local `node .../antigravity-mcp/dist/index.js` instead of `npx gemini-mcp-tool`, since antigravity-mcp is a custom local server, not an npm package). Model priority order updated in both docs to: `gemini-3.6-flash` → `gemini-3.1-pro` → `gemini-3.5-flash` → `gemini-3` → `gemini-2.5-pro` → `gemini-2.5-flash` (supersedes prior `gemini-3.1-pro-preview` → `gemini-3.5-flash-preview` → `gemini-3-pro` → `gemini-3-flash` order) |

| Pi deployment (hardening + PWA + VPN)  | done            | Raspberry Pi 4 (Debian 13 trixie) hardened: new `agent-runner` user (no sudo) holds Claude Code/Codex/OpenCode/Gemini CLIs + portable dev-MCP config, `rpcbind` disabled, `ufw` enabled (default-deny incoming, SSH allowed). App deployed via Docker: arm64 image built on host via `buildx`, Firefox/WebKit stripped from the Playwright base (only Chromium is ever launched — see `src/adapters/live/*Browser.ts`) and flattened via `docker export`\|`import` to actually shrink on-disk size (965MB→735MB; naive layer deletion doesn't reduce shipped size due to whiteouts). Inbound: Tailscale Serve (`https://rpi-dev.tail1e50ea.ts.net`, tailnet-only, no public exposure). Outbound: Gluetun container (`network_mode: service:gluetun`) routing through ProtonVPN OpenVPN — free-tier requires `FREE_ONLY=on` plus a genuinely fresh credential pair (undocumented in Gluetun's own error, only in community issues). **Gotcha:** the export/import flatten drops ALL base-image env vars unless restored via `--change`, not just app-specific ones — first pass missed `PLAYWRIGHT_BROWSERS_PATH=/ms-playwright`, silently breaking Migros/Lidl (Playwright looked in `~/.cache/ms-playwright` instead of the shared path); fixed by capturing the full `docker inspect --format '{{range .Config.Env}}'` list from the original image before flattening, not guessing. |
| Pi SD card migration (16GB → 128GB) | done | The 16GB card had become critically tight (~586MB free, 96% used) and was blocking further growth. Resolved by repurposing an idle 128GB microSD pulled from an old Android phone (confirmed beforehand: `public`/portable exFAT format, not Android's encrypted adoptable-storage format, so it's a plain, portable-anywhere card) rather than buying new hardware. Process: live `dd`+`gzip` clone of the running Pi's `/dev/mmcblk0` streamed over SSH/Tailscale to the PC (`sudo dd if=/dev/mmcblk0 bs=4M | gzip -1`, no downtime during the pull); written to the new card from Windows via **Raspberry Pi Imager**'s "Use custom" image option (declined its OS-customization prompts since this is a clone, not a fresh install — accepting them would have overwritten the existing hostname/user/SSH-key setup); Pi cleanly shut down over SSH before the physical card swap; booted fine on the new card on first try. Partition/filesystem then grown live to use the full card: `growpart /dev/mmcblk0 2` + `resize2fs` — **not** `parted resizepart`, which unconditionally refuses to touch a mounted partition (even with `-s`/script mode) and has no non-interactive override; `growpart` (from `cloud-guest-utils`) is the standard tool for growing an in-use root partition without unmounting or rebooting. Result: root filesystem now 117GB with 100GB free (11% used), up from 573MB free (96% used). **Gotcha:** `wsl --mount \\.\PHYSICALDRIVEn --bare` (the obvious Windows-side way to get raw `dd` access to the USB-attached card) fails unconditionally for USB SD-card readers specifically — long-standing, unresolved WSL2 bug (`Wsl/Service/AttachDisk/.../0x8007000f`, multiple open microsoft/WSL GitHub issues since 2021) — don't reach for it again for this kind of task, go straight to a native flashing tool (Raspberry Pi Imager / balenaEtcher / Win32DiskImager). Old 16GB card and a full pre-migration backup image (`D:\Pi_Backups\rpi-dev-boot-clone-2026-07-23-pre-128gb-migration.img.gz`, ~5.8GB compressed) both kept as rollback options. Verified end-to-end post-migration: Docker containers (`swiss-shopping`, `gluetun`) up and healthy, app responds `HTTP 200` on `/api/source-status`, Tailscale connectivity intact. |
| PWA dark-mode text contrast fix | done | `ProductSheet.tsx` and `SearchView.tsx` had product-name/price elements with no text color class, inheriting black from the browser default with no `dark:` override — invisible against `dark:bg-zinc-900` surfaces. Added explicit `text-zinc-900 dark:text-zinc-50`. |
| Reverse-geocode accuracy fix | done | `reverseGeocode()`'s static Zürich ZIP table (~25 hand-typed points, several sharing one generic city-center placeholder) mis-resolved real-world positions to the wrong postal code (e.g. Uetlihof → 8037 instead of the correct 8045/8055 area), which cascaded into wrong "nearest store" results. Added `reverseGeocodeAsync()` querying GeoAdmin's official PLZ locality registry first (mirrors the existing `resolveLocationAsync` forward-geocode pattern), falling back to the static table only on network failure. Wired into `GET /api/reverse-geocode`. 4 new tests. **Known remaining limitation:** the fix only corrects *which* PLZ centroid gets used — the browser's raw GPS coordinates are still discarded and converted to a PLZ text string immediately after reverse-geocoding, then re-resolved back to a centroid server-side. All downstream distance/nearest-store logic operates at postal-code-centroid granularity, not the user's exact position. Carrying raw lat/lon through end-to-end (using the PLZ string only for the display field) would need a small follow-up. |
| Nearest-first store ordering | done | `lookupAvailabilityByLocationProductsFirst` in `searchService.ts` returned stores in whatever order each vendor API gave them (effectively alphabetical), not sorted by actual distance from the user — despite already computing `userLat`/`userLon` for Migros store-finding. Added a `distanceBetween`-based sort immediately after fetching each chain's stores, before availability checks run. 1 new test. |
| Availability view: clickable products | done | `AvailabilityView.tsx` product cards had no click handler (unlike `SearchView.tsx`), so tapping a product on the availability screen did nothing. Wrapped the card content in a button, added `selected` state + `ProductSheet` render, matching `SearchView`'s pattern. |
| Nutrition tile dark-mode contrast fix | done | User-reported (real device screenshot, Android Chrome): nutrition `<dd>` values in `ProductSheet.tsx` had no text-color class, inheriting black with no `dark:` override — same recurring bug class as the earlier product-name/price fix. Fixed the `dd` directly, and added a body-level `color` fallback (light `#18181b`/dark `#fafafa` via `prefers-color-scheme`) in `pwa/src/index.css` so this class of regression can't silently recur on components that forget an explicit text color. Browser-verified in dark mode (headless, 390×844): nutrition values now render white-on-dark. |
| Windows npm script portability fix | done | `test:contract`/`test:live` were silently broken on Windows: embedded `VAR=1 vitest ...` syntax fails under `npm run`'s cmd.exe script-shell, and the `src/**/*.foo.test.ts` glob args only worked by accident when a POSIX shell pre-expanded them — vitest's CLI treats positional args as substring filters, not globs, so on Windows the literal glob string matched zero files ("No test files found", silently exiting 0). Added `cross-env` devDependency and replaced glob args with plain substring filters (`contract.test.ts`, `.live.test.ts`, `.integration.test.ts`). Verified: all 5 contract tests + all 7 live adapter smoke tests (8 chains) + 17 integration tests pass; full suite (720 tests), lint (0 errors), and build still clean. |
| Provider/central-index decision: superseded, accepted provisionally | done | The 2026-06-16 `SOURCE_PROVIDER_DECISION_RECORD.md` had explicitly *rejected* local runtime crawling for broad product search and left Migros/Coop "blocked — needs provider/index decision." Since then, every chain shipped as a live-beta Playwright-based local-crawling adapter anyway, backed by the 4-phase resilience overhaul (tiered cache freshness, circuit-breaker/budget-gated web-search failover, SQLite FTS5 catalog with lifecycle tracking, provenance/confidence scoring with implausible-price-swing rejection) — no paid provider or maintained index was ever engaged further. Got a second opinion from antigravity-mcp (gemini-3.6-flash) before closing this out: it agreed the reversal is justified given ~2 months of live-passing operation, but pushed back on treating it as a *permanently* solved question — flagged the anti-bot cat-and-mouse maintenance trap, Swiss UWG Art. 5/database-rights legal exposure, and silent-DOM-change degradation as real, undismissed risks. Recorded the decision as "accepted provisionally" with concrete re-evaluation triggers (sustained adapter-maintenance load, a real error-rate threshold, any legal/ToS notice, or a chain fully blocking the bypass) rather than declaring victory outright. Documentation-only change, no code touched. |
| Static-catalog decision: already resolved, dead code removed | done | Investigated whether production runtime still depends on static/fabricated catalog data (the exact concern `PRODUCTION_READINESS_AUDIT.md` raised on 2026-06-15: `createDefaultAdapters()` importing `STATIC_CHAIN_CATALOG`/`StaticChainAdapter` and returning invented data for unsolved chains). Verified it is **already fully resolved**, just never explicitly marked done in this tracker: `src/adapters/index.ts`'s factory is a clean exhaustive map of all 7 chains to real live adapters with zero static import; `STATIC_CHAIN_CATALOG`/`StaticChainAdapter` don't exist as files anywhere in the repo anymore; and the audit's own recommended guard tests (`index.test.ts` "not.toContain('StaticChainAdapter')", `index.integration.test.ts` "does not use StaticChainAdapter in default runtime") already exist and pass. The one thing that *was* still lying around: `src/adapters/migros.ts` + `migros.test.ts`, a 213-line **orphaned, never-imported** early HTTP-fetch adapter stub (targeting `api.migros.ch`, a domain that doesn't match any endpoint ever verified live — real Migros traffic is all under `www.migros.ch/...`) predating the `live/` adapter convention and fully superseded by `migrosLiveAdapter.ts`'s working Playwright-based implementation. It wasn't reachable from production (confirmed: zero importers), so it wasn't actually the audit's blocker, but it's dead weight that misleads readers about which Migros adapter is real — deleted it along with `src/util/http.ts` (`fetchJson`/`HttpError`), which had exactly these two dead files as its only remaining callers. 734 tests pass (13 fewer — the deleted dead adapter's own tests), lint 0 errors, build clean. |
| Migros promotions endpoint; Coop investigated and deferred | done | **Migros**: discovered via the (unused-elsewhere) `migros-api-wrapper` package's documented API-path tree that a genuine dedicated campaign feed exists — `product-display/public/web/v2/products/promotion/search` — and verified it live: returns a shared `startDate`/`endDate` campaign window plus bare `{id, type}` items, hydrated through the *same* product-cards pipeline regular search already uses (`fetchProductCards`), which already derives `price.original`/`promotionLabel` from `offer.promotionPrice`/`badges` (no new parsing needed there). Implemented `searchPromotions` following the exact structural pattern Denner's promotions adapter already established (cache with stale-fallback and one auth-retry, `calculateMatchStrength`-based text filtering/sorting, category/maxPrice filters, local `promotionAsProduct` shim) — added `MigrosParsedPromotion` + `toParsedMigrosPromotion` to `parsers/migros.ts`, replacing dead legacy `MigrosPromotion`/`MigrosPromotionsResponse` types left over from the static-catalog era that nothing referenced. Source registry and README matrix updated to `live-beta`. Live-verified end-to-end (real discount data, e.g. Migros' own "Königssalat" 3.70→3.20 CHF, 14%, real campaign dates) and fixed 3 MCP integration tests that had hardcoded `migros` as their "known-unsupported chain" fixture (swapped to `aldi`, which is still genuinely unsupported). **Coop investigated and deliberately deferred**: Coop's regular search API already returns `hasPromotion`/`originalPrice`/`listPromotions` inline (confirmed live across 5 queries — waschmittel, wasser, kaffee, fleisch all had real promo hits), so reusing that path was viable in principle, but Coop exposes **no promotion date-range field anywhere** — not on the product, not behind its `potentialPromotions`/`conditionalPromotionCodes` reference codes (every guessed detail-endpoint path 404'd) — and `NormalizedPromotion.validFrom`/`validUntil` are non-optional `Date` fields. Fabricating an estimated expiry the caller might reasonably act on ("deal ends Sunday") when the API doesn't actually say that would be inventing a fact, not estimating one, so Coop's `searchPromotions` stays an honest "not yet implemented" stub. 8 new tests (3 parser-level, 5 adapter-level incl. stale-cache fallback); 747 tests pass, lint 0 errors, build clean. No PWA UI surface exists for `search_promotions` (MCP-tool/backend-only feature) so verification was via live end-to-end script calls plus the (also-live) MCP integration suite, not a browser check. |
| Volg/Otto's size extraction; Aldi investigated and ruled out | done | Investigated all three live against the real APIs. **Volg** (WooCommerce Store API): `weight`/`formatted_weight` fields exist but are always unpopulated (`""` / `"n. v."` on every product checked) — pack size only appears embedded in the free-text `name` (e.g. `"Kondensmilch 300g"`, `"Kinder Milchschnitte 5x28g"`). **Otto's** (OCC API `fields=FULL`): no size/weight field at all in the schema; same embedded-in-name pattern (e.g. `"Mulino Bianco Girotondi 800 g"`). Added `extractTrailingSize()` (`src/util/productSize.ts`) — matches a trailing `(NxM )?number+unit` token (g/kg/ml/cl/dl/l), splits it into a `size` field, and strips it from the display name so it isn't shown twice; wired into both parsers' live code paths (`parseVolgWooCommerceResponse`, `parseOttosOccProduct`) and the `NormalizedProduct` mapping in both live adapters. **Aldi ruled out**: JSON-LD `Product.name` across 3 categories checked (milk, croissant, water) never includes size, and there's no `additionalProperty`/weight field either — the only place pack size might exist is an internal Nuxt/devalue-serialized state blob (reference-indexed array format), which would need a real deserializer to parse reliably and is exactly the kind of undocumented-format guesswork this codebase avoids elsewhere (no fake fallbacks). Browser-verified in the live PWA: Volg cards now show `"Vollmilch UHT · CHF 1.15 · 5dl"` and `"Milchreis Vanille · CHF 1.60 · 175g"` instead of the size buried in the title. 14 new tests (10 for the shared util incl. a "mid-name number without unit isn't a size" case, 2 each for Volg/Otto's live-path parsing); 741 tests pass, lint 0 errors, build clean. |
| Migros nutrition per-100 column fix | done | The nutrition-enrichment code blindly read `values[0]` from Migros's MGB `nutrientsTable`, assuming the first column was always the per-100g/100ml basis (the UI labels it "Nährwerte (pro 100g)" unconditionally). Verified live against the real API across 7 categories (milk, chocolate, chips, yogurt, apples, water, rice): every sample so far does lead with a `"100 g"`/`"100 ml"` header, with any per-portion column (e.g. `"1 Glas (250 ml)"`) second — but that's an unvalidated assumption baked into fixed-index access, not a documented guarantee. Fixed to locate the per-100 column explicitly by matching its header (`/^100\s*(g|ml)$/i`) rather than assuming index 0, and to omit nutrition entirely (rather than mislabel per-portion data as per-100g) when no such column exists. **Found and fixed real test-coverage gap while implementing this:** this entire enrichment code path had zero test coverage — `fetchProductDetail` wasn't in the module's test mock at all, so every existing test silently caught-and-ignored a "not a function" error inside enrichment's try/catch, meaning nutrition output was never actually exercised by any test. Added the missing mock plus 3 new tests: standard column order, a deliberately reversed order (per-portion column first) to prove the fix locates by header rather than position, and a no-per-100-column case proving nutrition is correctly omitted rather than mislabeled. 727 tests pass, lint 0 errors, build clean. |
| Raw GPS precision threaded end-to-end | done | Previously the browser's raw GPS position was reverse-geocoded to a PLZ display string immediately, then that string was the *only* thing sent to the server, which re-geocoded it forward to a postal-code centroid for all nearest-store distance math — real position was discarded twice over. Added optional `latitude`/`longitude` to `StoreAvailabilityByLocationFilters`; `lookupAvailabilityByLocationProductsFirst` now uses them directly (skipping the forward-geocode) when present, which also improves precision for the Migros/Coop `userLatitude`/`userLongitude` availability-fallback path that already consumed these values. PWA: `AvailabilityView.tsx` sends `userCoords` alongside the display text when the user tapped "use my location", and clears them the moment the location text is hand-edited afterward (so stale GPS from a previous fix can't get attached to an unrelated typed location). **Scope decision:** each chain's own store-*finding* geocoding (Migros/Coop/Aldi/Otto's resolving the location text themselves to search their own store-locator APIs) was deliberately left untouched — wiring raw coords into that step too would require per-chain cache-key changes (today keyed on the location string, intentionally shared across nearby users) to avoid effectively disabling that cache, for a gain that's marginal given Swiss postal codes are already small and store-search radii are generous. The concrete, user-visible "nearest store" precision problem was specifically the sort/ranking math, which this fully addresses. New regression test proves explicit coordinates override geocoding even when they'd disagree; browser-verified end-to-end with mocked `navigator.geolocation` (captured actual fetch payload showing `latitude`/`longitude` alongside `"3011 Bern"`, and confirmed they're dropped after a manual location edit). 724 tests pass, lint 0 errors, build clean. |
| SerpAPI spelling-fix retry | done | Verified live against the real SerpAPI key (not just docs): Google/SerpAPI already silently substitutes corrected-spelling organic results for many typos without ever reporting zero results (e.g. "zanhcreme"/"yoghurrt"/"schockolade"/"kafee bohnen"/"brotaufstrih" all returned 10 organic results directly, state `"Some results for exact spelling but showing fixed spelling"` — no code change needed for these, they already worked). The real remaining gap is narrower: queries that return truly zero organic results *and* SerpAPI/Google offers a correction anyway via `search_information.spelling_fix` (state `"Empty showing fixed spelling results"`). Added a one-shot retry in `SerpApiProvider`: on zero results, if `spelling_fix` is present and differs from the original query, retry once with the correction before giving up. **Gotcha caught via live testing, not doc-reading:** `spelling_fix` echoes back the *entire* `"site:<site> <query>"` string we sent, not just the corrected term — an initial implementation based on the documented schema alone would have double-prefixed the site restriction (`site:x site:x query`) on retry; fixed by stripping the known, self-constructed site prefix before reusing the value. Queries with no exact-index match *and* no Google-offered suggestion at all (state `"Fully empty"`, e.g. "zahnkremee") remain genuinely unfixable without building real local spell-checking, which is out of scope — this only reuses correction data the upstream API already computed. Scoped to `SerpApiProvider` only (first/most-used provider in the auto fallback chain); HasData/Searlo/Firecrawl don't document an equivalent field. 2 new tests (retry-and-strip-prefix case, no-op-when-no-suggestion case); 723 tests pass, lint 0 errors, build clean. |
| Per-product availability passthrough | done | Root cause of the known "shared availability" gap: `StoreProductAvailabilityFilters` had no way to pass a specific product through, so Migros/Coop/Aldi's `lookupStoreProductAvailability` always re-resolved the raw `query` text via each chain's own (weak) search to "find a product ID" — and because `searchService.lookupAvailabilityByLocationProductsFirst` ran this check once per chain (not per product) and fanned the single result out to every representative product of that chain via `.filter(s => s.chain === product.chain)`, every product shown for a query displayed identical store availability, regardless of which specific product it actually was. Added optional `product?: NormalizedProduct` to `StoreProductAvailabilityFilters`; Migros/Coop/Aldi adapters now use the caller-supplied product directly when present, skipping their internal search-by-text step (Lidl/Volg/Otto's remain unsupported stubs, unaffected). `searchService.ts` restructured to loop per representative product within each chain (reusing the same nearest-first-sorted store list already fetched once per chain) and key results in a `Map<NormalizedProduct, StoreWithProductAvailability[]>` instead of a shared array — each product's stores now come from its own check. New regression test proves per-product isolation (`searchService.test.ts`: "checks availability per product rather than sharing one chain-wide result"). Trade-off: this multiplies availability-check calls by the number of representative products per chain (up to 5) instead of one shared call — an intentional, scoped cost for correctness; no vendor exposes a batch-by-product-list endpoint to avoid it. 721 tests pass, lint 0 errors, build clean; browser-verified end-to-end against live Migros/Coop data with no regressions. |
| antigravity-mcp model-order self-contradiction fix | done | `CLAUDE.md`/`AGENTS.md` said both "prefer this model order" and "do not specify a model unless asked" — self-contradictory, and following the second line meant the priority order never actually got applied (antigravity-mcp's `sdk` backend silently defaulted to `gemini-3.5-flash`, unrelated to exhaustion of higher-priority models). Fixed wording: always pass `model` explicitly starting from the top of the list. |
| PWA design pass (header, brand color, font) | done | Implements the two highest-impact items from the 2026-07-23 antigravity-mcp design audit, scoped down from its exact suggestions: (1) header changed from a static left-aligned banner to `sticky top-0` with `backdrop-blur-md` + translucent background, matching the treatment the bottom nav already used; (2) brand accent moved off the generic Shadcn `emerald`/`zinc` default to `blue`, **not** the audit's suggested `red-600`/`orange-500` — those are literally Coop's and Migros' brand colors and this app is a neutral cross-retailer comparison tool. `emerald` was kept everywhere it carries actual data meaning (badge `success` variant = "Verfügbar", `AvailabilityView` open-status dot) so brand and status color no longer share one hue; promo badge (discount callout) moved to `amber-600` solid so it reads as a distinct "deal" tag rather than reusing whichever hue happens to be the button color; (3) font stack dropped the unused `"Inter"` webfont reference (never actually loaded — no `<link>`/`@font-face` anywhere, so it silently no-opped to system fonts already, but the stated intent was wrong) for an explicit native stack (`-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica, Arial, sans-serif`). `theme-color` meta updated to match (`#2563eb`). Browser-verified at 390×844 in dark mode: header sticky/blurred on scroll, wordmark/tab-indicator/selected-chips/buttons all blue, green "Verfügbar" badges and open-status dots unchanged and visually distinct from the new brand color. 720 tests pass (no PWA visual/token tests exist in this repo to add to), lint 0 errors, build clean. Light-mode not re-verified (no `dark:`-only styling touched — same classes as the emerald they replaced). |
| Availability location pill + map view | done | Follow-up to the design pass: location only matters on the Availability tab (Search has no location concept), so the "location selector" idea from the design audit was scoped down to live entirely inside `AvailabilityView.tsx` rather than lifted into the shared `App.tsx` header — no cross-tab state needed. Sticky sub-header (positioned via `top-[calc(env(safe-area-inset-top)+3.25rem)]`, matching the app header's now-fixed `h-[calc(env(safe-area-inset-top)+3.25rem)]` height) holds a location pill (tap to re-edit; a "Fertig"/check button closes edit mode without resubmitting once a location is already set) plus a list/map toggle that appears once results exist. Map view is a new `AvailabilityMap.tsx` using `leaflet`/`react-leaflet` (not Google Maps — no API key/billing needed for embedding; store `mapsUrl()` links still go to Google Maps) with per-store markers aggregated across all matched products (dedup by store id), color-coded green/red/gray by availability, a distinct blue user-location marker, and a CSS filter-invert dark-mode tile trick scoped to `.leaflet-tile-pane` only (markers/popups stay unaffected). Lazy-loaded via `React.lazy`/`Suspense` so leaflet (~156KB) only downloads when a user actually opens map view — keeps the main bundle at ~480KB instead of pushing it past Vite's 500KB chunk-size warning. **Bug found and fixed during testing:** the initial implementation used a `viewMode === 'map' ? <Map/> : <List/>` ternary, which unmounts/remounts the whole subtree on every toggle; this left the results list's framer-motion stagger-in animation permanently stuck at `opacity:0` after a map→list round trip (confirmed via `getComputedStyle`, not just visually) and would have reset the map's pan/zoom on every visit. Fixed by keeping both trees permanently mounted and toggling visibility with a `hidden` class instead, which also fixes Leaflet's "container was hidden via CSS" stale-size issue via an `useMap()`-based `invalidateSize()` call on becoming visible again (`InvalidateSizeOnShow`). Verified in two environments: headless browser at 390×844 (dark mode: sticky pill/toggle bar, map markers + popups, list↔map round trip with no stuck animation, map state preserved across toggles) and the user's actual physical Android phone over `adb reverse`/Chrome (confirmed real-device rendering of the sticky header, pill collapse/expand, and map tiles/markers). 720 tests pass, `tsc -p pwa` clean, `build:pwa` clean (main bundle 480KB / map chunk 157KB, split confirmed). New deps: `leaflet`, `react-leaflet`, `@types/leaflet` (devDependencies, matching how other PWA-only runtime libs are declared in this repo). Not addressed: `pwa/`'s TypeScript files aren't covered by the repo's `npm run lint` (scoped to `src` only), so this change was only checked by `tsc` — pre-existing repo condition, out of scope here. |

| Self-hosted CI/CD deploy pipeline for the Pi | done | Self-hosted GitHub Actions runner registered directly on the Pi (`grapeberry-dev`, `docker` group, systemd service) — native arm64 build, no QEMU cross-compile. `.github/workflows/deploy.yml`: manual `workflow_dispatch` (deploy is a deliberate decision, not automatic on merge), re-runs lint/test/build for the dispatched ref, builds the image, flattens it via `scripts/docker-flatten.sh` (docker export/import shrink, restoring the full source image's env/cmd/workdir/user/expose/volume config that `docker import --change` drops by default — known limitation: `--change` has no HEALTHCHECK support, so `docker-compose.yml` and the new tracked `deploy/docker-compose.pi.yml` both define the healthcheck at the compose level instead), tags the previous image for rollback, syncs `deploy/docker-compose.pi.yml` onto `/srv/swiss-shopping-mcp/docker-compose.yml` (that file is now pipeline-owned — edit the tracked copy, not the one on the Pi), deploys, and health-checks with `scripts/deploy-healthcheck.sh` (automatic rollback to the previous image tag + redeploy on failure). `.gitattributes` pins `*.sh` to LF so scripts don't break on the Linux runner regardless of the committing machine's line-ending config. **Real bug found and fixed along the way:** the pipeline's lint/test/build gate caught that `src/services/spaRegression.test.ts` had been failing on GitHub-hosted CI for the prior 3 pushes to main (unrelated to this session's other changes) — it calls `createDefaultAdapters()` with no mocking but, unlike every `*.live.test.ts` file, had no `describe.skipIf(process.env.LIVE_SOURCE_TESTS !== '1')` guard, so it ran live vendor calls unattended in default `npm test`. Confirmed live: Migros's abuse-detection layer blocks requests from a non-VPN-routed egress IP — the deployed app (routed through Gluetun/ProtonVPN) returns real Bern store data for the exact same query that returned zero from both the GitHub-hosted runner and the Pi's bare self-hosted runner. Gated the 8 live-dependent `describe` blocks the same way `.live.test.ts` files are; left the 3 blocks that don't hit real adapters (source-status metadata, param validation, unknown-chain graceful degradation) ungated. First real end-to-end pipeline run succeeded: 719 tests pass, lint 0 errors, image built+flattened+deployed, health check green, confirmed via a fresh `pi-slim` image timestamp on the Pi.

| PWA design refresh v2: warm-stone/brass token system | done | Full visual identity pass to remove the "generic AI-generated shadcn app" look flagged in an antigravity-mcp (gemini) design audit. Replaced the prior blue-accent/zinc-neutral palette with a warm-stone neutral scale plus a bronze/brass brand accent (`--brand`/`--brand-ink` and a `--surface`/`--surface-sunken`/`--ink`/`--faint` semantic token set) in `pwa/src/index.css`, with a matching dark-mode override block and a `prefers-color-scheme` media-query fallback for browsers without a manual toggle. Added `@utility` Tailwind v4 shadow classes (`shadow-card`, `shadow-inset`, layered rim-light effects) so components reference semantic shadow tokens instead of ad hoc `shadow-*` values. `Card`, `Badge`, `Button`, `Input` in `pwa/src/components/ui/` refactored onto the new tokens; `Badge` gained a `promo` variant styled as a stamp-like discount callout distinct from the brand hue (recurring lesson from the earlier blue/emerald collision — semantic and brand color must stay visually distinct). New `pwa/src/components/ui/price.tsx`: Swiss shelf-tag-style price component rendering the rappen (cents) as a smaller trailing superscript (`CHF 1.⁸⁵`), fixing an earlier vertical-alignment complaint. Receipt-style CSS utilities (`.tear`, `.barcode`) added for domain-grounded visual flavor. Verified in both dark and light mode (headless browser, 390×844) via `--blink-settings=preferredColorScheme=0/1` Chromium launch flag (no in-app theme toggle exists; the app follows OS `prefers-color-scheme` only) — contrast and token application confirmed correct in both modes across all four tabs. |
| PWA design refresh v2: MapLibre GL migration + product maps | done | Replaced the `leaflet`/`react-leaflet` stack (added only the day before for the availability map view) with `maplibre-gl` + free swisstopo `swissimage` tiles — no API key/billing dependency, native two-finger rotation, and visually distinct from generic OpenStreetMap-styled Leaflet output. Deleted `pwa/src/components/AvailabilityMap.tsx`; added `pwa/src/components/ProductMap.tsx`, which moves the map from a shared multi-product availability view into the per-product `ProductSheet` — scoped to that product's own nearby-store markers instead of an aggregated cross-product view. Retains street/satellite toggle. Browser-verified end-to-end (dark mode, 390×844): opening a product from the Availability tab shows a MapLibre map with green store markers clustered around Zürich HB, street/satellite toggle, and correct dark-tile styling; confirmed the map survives the `ProductSheet` open/close cycle without the earlier Leaflet "hidden via CSS" stale-size bug recurring. |
| PWA design refresh v2: vendor capability popover | done | New `pwa/src/components/VendorBadge.tsx`: replaces the plain chain-name `Badge` on product cards with a Radix `Popover`-based trigger showing that chain's live `get_source_status` capability matrix (Produktsuche/Angebote/Filialsuche/Verfügbarkeit/Nährwerte, each `live-beta`/`unsupported`/etc.) plus latency. Single-open-at-a-time pattern: each card's popover open state is lifted to the parent view (`openVendor` state keyed by `chain:productId` in `SearchView`/`AvailabilityView`/`CompareView`) so opening one badge closes any other. Uses Radix's built-in Floating-UI-based collision/flip handling for viewport-edge placement rather than a custom positioning implementation. Browser-verified: popovers opened on both a left-edge card (x≈28px) and a right-edge card (x≈261px in a 390px-wide viewport) both rendered fully on-screen with no clipping, in both dark and light mode; verified the single-open-at-a-time close-on-new-open behavior. **Gotcha hit during verification:** synthetic `.click()` calls via `execute_script` on a Radix popover trigger can spuriously toggle-close on a second rapid script-dispatched click (likely a pointerdown/click event-ordering mismatch specific to CDP-driven synthetic events, not a real user-interaction bug) — resolved during testing by using the browser tool's native `click_element` instead of scripted `.click()` for popover interactions. |
| PWA design refresh v2: CompareView + StatusView tabs | done | Two new top-level tabs added to the 2-tab (Availability/Search) nav, now a 4-tab `App.tsx` layout (`In der Nähe` / `Suche` / `Vergleich` / `Status`, icons via `lucide-react`). `pwa/src/components/CompareView.tsx`: product-name + quantity input UI over the existing `compare_prices` backend logic, ranked results with a `BESTPREIS` badge on the top offer, animated `@number-flow/react` result count ("N Angebote in Xs"). `pwa/src/components/StatusView.tsx`: per-chain capability + real measured latency cards backed by `get_source_status`/`get_metrics`, explicitly labeled "Fähigkeiten sind deklariert (hand-gepflegt), nicht live geprüft" to avoid implying the capability matrix itself is live-verified (only the latency/cache numbers are). `App.tsx`'s `--nav-h` `ResizeObserver` measurement (existing mechanism from the prior nav-overlap fix) needed no changes to accommodate the extra tabs — confirmed by browser-verifying that the last row of a 12-result Search list scrolls fully clear of the bottom nav with visible spacing, both before and after this tab addition. Browser-verified end-to-end (dark and light mode, 390×844): live `Milch` comparison returns 7 ranked offers with correct best-price highlighting; Status tab renders real per-chain latency bars and capability dots for all 8 chains. 719 backend tests pass, `tsc -p pwa --noEmit` clean, `npm run build` and `npm run build:pwa` clean, lint 0 errors (23 pre-existing warnings, unchanged). Version bumped to `0.2.0` (first tracked version bump; prior work shipped under the initial `0.1.0`). |

| Client-side filter scoping (item 18): already correct, verified and documented | done | Item 18 of `PWA_REALDEVICE_FEEDBACK_2026-07-25.md` asked whether narrow-only filters (in-stock, open-now) re-query the backend unnecessarily. Investigated per antigravity-mcp's (gemini-3.6-flash) suggested scoping (client-side: in-stock/open-now/discount/price-range; server-side: chain selection, location, query text) — **found already correctly implemented**, no code change needed: `AvailabilityView.tsx`'s `inStockOnly`/`openNow` are plain component state, excluded from the TanStack Query `queryKey` (`['availability', params]`, where `params` only changes on form submit), and `results` is a derived `const` computed fresh on every render from the last-fetched `data` plus the current filter state — toggling either filter re-renders instantly with no network call. Chain selection correctly stays server-side (only takes effect on next submit, since it widens/narrows which vendor APIs get queried). Browser-verified: after an "8 Ergebnisse in 9.0s" search, toggling "Nur verfügbar" instantly updated to "7 Ergebnisse" with the **same unchanged "9.0s"** elapsed-time label — proof no new fetch occurred. `SearchView.tsx`/`CompareView.tsx` don't currently have any narrow-only filters (only chain selection, which is correctly server-side), so there was nothing to change there either. |
| CompareView quantity stepper | done | Item 16 of `PWA_REALDEVICE_FEEDBACK_2026-07-25.md`, scoped per antigravity-mcp (gemini-3.6-flash) second opinion: relabeled "Menge" → "Anzahl Packungen (gilt für alle Produkte)" to make the flat pack-count-multiplier semantics explicit; replaced the raw `<input type="number">` with a `[-] [value] [+]` stepper (`setClampedQuantity` clamps 1-99, direct keyboard entry still works, +/- buttons disable at the clamp bounds). Deferred per antigravity's recommendation: surfacing `comparisonBasis: 'unitPrice'` in this UI — real unit-conversion edge cases (100g vs 1L) need their own design pass, not bundled into this fix. Browser-verified: stepper increments/decrements, disables at min, value updates live. |
| Toast offset/swipe fix (nav overlap) | done | Item 24 of `PWA_REALDEVICE_FEEDBACK_2026-07-25.md`, found in a follow-up real-device test pass. Root cause: `App.tsx`'s `Toaster offset={{ bottom: 'calc(var(--nav-h) + 0.75rem)' }}` had no CSS fallback for `--nav-h` — if unset at evaluation time the whole `calc()` is invalid and sonner silently uses its own default offset, unaware of the fixed nav, reproducing the reported overlap. Separately, sonner's default `swipeDirections` for `position="bottom-center"` resolve to `['bottom', 'center']` (`'center'` isn't a real direction), leaving only an awkward, edge-of-screen downward swipe. Fixed: `calc(var(--nav-h, 4.5rem) + 0.75rem)` fallback + explicit `swipeDirections={['bottom', 'left', 'right']}`. Browser-verified the gap fix directly (headless, 390×844, mocked geolocation): toast now renders with clear separation above the nav. Swipe-dismiss itself could not be confirmed in this environment — synthetic CDP `PointerEvent` dispatch doesn't register with sonner's internal gesture handler, a known limitation class already documented elsewhere in this tracker (VendorBadge popover synthetic-click gotcha); the fix is a standard, low-risk sonner API usage, but real-device confirmation of the swipe gesture specifically is still pending. |
| CompareView cards open ProductSheet on click | done | Item 17 of `PWA_REALDEVICE_FEEDBACK_2026-07-25.md`. Wrapped `CompareView.tsx` row content in a button (`onClick={() => setSelected(offer.product)}`), added `selected` state + `<ProductSheet>` render, matching the existing `SearchView`/`AvailabilityView` pattern exactly — CompareView was the only card list without a tap-to-detail affordance. Browser-verified: `Milch` comparison → tapping the #1 Bestpreis card opens the sheet with image/price/nutrition/vendor link. |
| Real-device feedback pass: live search progress/ETA + query/location autocomplete (items 15, 19, 20) | done | Closes the last three open items from `docs/active/PWA_REALDEVICE_FEEDBACK_2026-07-25.md` Phase 3 (item 25/aldi-now.ch deliberately deferred to a future session — user-scoped as low priority, comparable effort to a full new adapter build). **Item 15 (live progress/ETA):** `SearchService.searchProducts` gained an optional `SearchProductsOptions.onChainProgress` callback, fired synchronously as each adapter's `Promise.all`-parallel vendor search resolves (real completion order, not request order) — carries `{chain, ok, elapsedMs, respondedCount, totalCount}`. New `GET /api/search-products/stream` SSE endpoint in `server.ts`: `init` event (chain count + per-chain ETA sourced from `MetricsCollector`'s real measured latency history, `undefined` when no history exists yet — never a fake `0s`/`?`), `progress` event per chain, final `done` event with the same envelope shape the POST endpoint returns. `SearchView.tsx` replaced its `searchProducts` POST call with the new `streamSearchProducts` (EventSource-based) client function, rendering "X/Y Händler geantwortet" with a live countdown (`setInterval`-ticked, counting down from the init ETA) during the fetch; the existing post-completion "N Ergebnisse in Xs" line is unchanged. **Items 19/20 (query + location autocomplete):** new `CatalogService.suggestProductNames()` does an FTS5 prefix-match (last typed token as `term*`, earlier tokens exact) against the existing `products_fts` index — purely derived from real products the catalog has already observed via past searches, no hardcoded list; backs `GET /api/query-suggest`. New `geo.suggestLocationsAsync()` reuses the same GeoAdmin SearchServer API `resolveLocationAsync` already calls, requested with `limit>1` instead of `limit=1`; verified live against the real API for both zip-code prefixes (`"800"` → `"8001 Zürich"`, `" - "` separator normalized to a space) and place-name prefixes (`"wint"` → `"Winterthur"`/`"Winterthour"`/`"Wintersingen"` via the `gg25` origin, trailing `"(ZH)"` canton suffix stripped); backs `GET /api/location-suggest`. New shared `pwa/src/components/ui/suggest-input.tsx` (`SuggestInput`): debounced (250ms, 2-char minimum) dropdown wrapping `Input`, keyboard nav (arrows/Enter/Escape), click-outside close, stable-function-reference contract for `fetchSuggestions` to avoid debounce-effect churn. Wired into all four applicable fields: `SearchView`/`CompareView`/`AvailabilityView` query inputs (`suggestQueries`) and `AvailabilityView`'s location field (`suggestLocations`, with `onSuggestionSelect` looking up the matching suggestion's lat/lon to populate `userCoords` directly — reuses the existing raw-GPS-precision threading path, skipping the geocode-check round trip since a picked suggestion is already known-good, same as a GPS fix). **Real bug caught during mandatory browser verification, not just reasoning about it:** selecting a suggestion called `onChange(suggestion)`, which changed the `value` prop, which re-triggered `SuggestInput`'s own debounced suggestion-fetch effect (dependency on `value`) — after the 250ms debounce elapsed, the dropdown would silently reopen with fresh suggestions for the just-selected text (confirmed live: `aria-expanded` flipped back to `"true"` about a second after selecting "Winterthur"). Fixed with a `justSelectedRef` guard that skips exactly one post-selection effect run. **Second test-environment gotcha (not a product bug):** the rebuilt PWA bundle initially kept being served stale after `npm run build:pwa` — root cause was the browser's standard HTTP cache holding the previous `index.html` (its content-hashed script tag still pointed at the old JS bundle filename), *not* the service worker (0 registrations/caches found on inspection) — `serveStaticFile` in `server.ts` sends no `Cache-Control` header at all for any static asset, `index.html` included; worked around for this session via a cache-busting query string on manual reloads, left as a known gap (a redeployed Pi instance could theoretically serve a stale `index.html` to an already-cached browser tab until a hard refresh) rather than fixed, since it's outside this session's scope. New tests: 2 `SearchService` progress-callback cases, 6 `CatalogService.suggestProductNames` cases, 6 `geo.suggestLocationsAsync` cases (mocked `fetch`), 5 new `server.integration.test.ts` cases (SSE event framing/ordering/chain-scoping, query-suggest happy path + below-minimum-length). 750 tests pass (up from 731), lint 0 errors (23 pre-existing warnings, unchanged), `tsc -p pwa --noEmit` clean, both builds clean. **Browser-verified live end-to-end** (dark + light mode, 390×844, real backend hitting real vendor APIs — not mocked): query autocomplete dropdown in all three views showing real catalog-derived suggestions (e.g. "Vollmilch", "Vollmilch UHT 3.5% IP-Suisse 6x1l", "Emmi Bio-Vollmilch"); location autocomplete showing real GeoAdmin place-name/zip results; a full live 7-chain search via the SSE endpoint showing genuinely staggered real per-chain response times (migros 6ms → lidl 10610ms) with the UI's "6/7 Händler geantwortet · noch ~1s" counter updating live, settling into the existing "12 Ergebnisse in 12.3s" completion line; post-fix confirmation that dropdown-reopen-after-select no longer occurs. |
| Web search provider chain rework: DDG-primary, paid providers opt-in only | done | Implements the 2026-07-25 decision recorded in `docs/active/PWA_REALDEVICE_FEEDBACK_2026-07-25.md` item 23. `src/sources/webSearch.ts`: added `DuckDuckGoLiteProvider` (scrapes `lite.duckduckgo.com` — verified live: a table-based HTML4 layout with single-quoted `class='result-link'` anchors, distinct from `html.duckduckgo.com`'s `result__a`, though the same `//duckduckgo.com/l/?uddg=` redirect scheme; `parseDuckDuckGoHtml` generalized with an optional `resultLinkClass` param rather than duplicating the parser). `buildAutoComposite` rewritten: default `auto` chain is now `ddg` (html, primary) → `ddg-lite` (secondary) → `google` (dormant tail, only if legacy CSE keys are configured); SerpAPI/HasData/Searlo/Firecrawl removed from the default chain (classes and explicit `SWISS_SHOPPING_WEB_SEARCH=serpapi\|hasdata\|searlo\|firecrawl` modes kept for opt-in use, never reached by `auto`). Added a short in-memory TTL query cache to `CompositeWebSearchProvider` (keyed by sorted site list + normalized query, default 60s) to absorb duplicate bursts against the now-primary rate-limit-sensitive DDG providers — **opt-in, default disabled** (`cacheTtlMs` defaults to 0) and only enabled explicitly by `buildAutoComposite`, so direct `CompositeWebSearchProvider` construction (tests, future callers) keeps today's per-call semantics unless it asks for caching. **Real regression caught by the existing test suite, not just reasoning about it:** a naive "cache enabled by default" first draft broke `phaseB.test.ts`'s circuit-breaker trip test, which fires 3 identical repeated queries expecting 3 real provider invocations — the cache silently served 2 of them from the first result, so the breaker's failure counter never reached its threshold. Fixed by making the cache opt-in rather than weakening the test's intent. README and `docker-compose.yml` env-var docs updated to describe the new default chain and mark the four paid-provider keys as opt-in-only. 15 new/updated tests in `webSearch.test.ts` (DuckDuckGoLiteProvider fetch/challenge/throttle, generalized parser, auto-chain composition never including paid providers even when keys are present, live html→lite failover, and 6 cache-behavior cases including the disabled-by-default case and the real `buildAutoComposite` cache-enabled case); 731 tests pass, lint 0 errors, build clean. **Browser-verified live** (not just mocked): started the server, ran a real semantic query ("toothpaste for sensitive teeth") through the actual PWA Search tab — 6 real Migros results in 22.5s; `GET /api/metrics` confirmed the real chain executed exactly as designed (`requestsByProvider: {ddg, ddg-lite, google}` all attempted, 1 real DDG→next-provider fallback, a circuit breaker legitimately opening under real DDG flakiness from this dev machine's IP, with the request still succeeding end-to-end via the lite/google fallback). Direct `curl` to `html.duckduckgo.com` from this same machine returned a live HTTP 202 bot challenge at test time while `lite.duckduckgo.com` returned real 200 results — concrete, real-time evidence for why the lite fallback earns its place, not just a theoretical hedge. |

| Search-all-vendors empty-state nudge | done | User request: when the chain filter is narrowed (not all chains selected) and a search/availability check returns zero results, show an inline "Bei allen Händlern suchen" button in the empty state that widens the chain selection to the full set and re-runs the same query. `SearchView.tsx`: button appears when `chains.length < ALL_CHAINS.length` and `visibleProducts.length === 0`; calls `setChains([...ALL_CHAINS])` + re-submits via `setSubmitted`. `AvailabilityView.tsx`: same pattern with `AVAILABILITY_CHAINS` (migros/coop/aldi) and `setParams`; empty state also gets an explicit note that only Migros/Coop confirm real stock (per antigravity-mcp gemini-3.6-flash review) so widening to Aldi doesn't read as if it adds verified availability. No backend changes — reuses the existing chain-toggle state and submit/query mechanisms already in place. Browser-verified end-to-end (headless, 390×844): SearchView case forced a genuine zero-result state (Otto's-only + "Bratwurst"), confirmed the button, clicked it, confirmed all 7 chips became selected and real Migros/Aldi/Lidl bratwurst results appeared; AvailabilityView case forced zero results via the existing client-side "Nur verfügbar" filter against Aldi (whose availability API is always unsupported/false), confirmed the note+button, clicked it, confirmed Migros+Coop+Aldi all selected and real in-stock Migros results appeared. 750 tests pass (unchanged — PWA has no component test suite in this repo, matching prior UI-only fixes), lint 0 errors (23 pre-existing warnings, unchanged), both builds clean. Also drafted `docs/active/SHOPPING_LIST_AGENT_PLAN.md` (reviewed with antigravity-mcp) for a separate, not-yet-implemented shopping-list/RAG-agent feature — see that doc and the "Next tasks" entry below. |
| Chat agent implementation | done | Built the PWA chat agent designed in `docs/active/CHAT_AGENT_ARCHITECTURE_PLAN.md`: installed `ai@6.0.237`, `@ai-sdk/react@3.0.239`, `@openrouter/ai-sdk-provider@2.10.0` — API surface (`tool({inputSchema, execute})`, `streamText({stopWhen: stepCountIs(n)})`, `pipeUIMessageStreamToResponse`, OpenRouter's `models: string[]` fallback field, `useChat({transport: new DefaultChatTransport({api})})`) confirmed by reading the installed packages' own `.d.ts` files directly rather than trusting web docs, since this is bleeding-edge (2026) tooling outside training-data knowledge. Re-verified the primary/fallback/emergency model IDs still exist and are free via a live `GET /api/v1/models` call before writing any code. `src/agent/tools.ts`: wraps all 8 `executeToolCall` tools as AI SDK tools, reusing the exact same zod schemas `src/tools/handlers.ts` already had (newly exported, not duplicated); strips `nutrition`/`ingredients` before results re-enter the model's context; wraps thrown exceptions as `{error}` tool results instead of crashing the stream; enforces a 24-call-per-turn tool budget. `src/agent/chatAgent.ts`: `runChatAgent()` — grounding-discipline system prompt, `stopWhen: stepCountIs(12)`, `abortSignal: AbortSignal.timeout(15000)`, model param injectable for tests. `src/web/server.ts`: stateless `POST /api/chat` piping `result.toUIMessageStream()` to the raw Node `ServerResponse`. `pwa/src/components/ChatView.tsx`: new lazy-loaded (code-split, matching the existing `ProductMap` precedent — keeps the ~230kB-gzipped AI SDK out of the main bundle) Chat tab using `useChat`; renders tool-result parts as distinct product/compare/store cards separate from prose (grounding requirement), reusing `ProductSheet` on card tap; `pwa/src/lib/chatHistory.ts` persists history to IndexedDB (backend is stateless per the plan's "Session / history state" decision). Tests: `src/agent/tools.test.ts` (3, dispatch/error-wrapping/budget), `src/agent/chatAgent.test.ts` (2, `ai/test`'s `MockLanguageModelV3` driving a real tool call end-to-end — discovered and worked around a real off-by-one in `MockLanguageModelV3`'s array-based `doStream` indexing, confirmed against the installed source, not assumed), `src/agent/chatAgent.eval.test.ts` (10 golden-eval fixtures against the real model, opt-in via `RUN_AGENT_EVAL=1`/`npm run test:eval`), `src/agent/chatAgent.live.test.ts` (1 opt-in live smoke test, `LIVE_SOURCE_TESTS=1`, matches existing `.live.test.ts` convention). **Real bug caught by mandatory browser/manual verification, not just reasoning about it:** the primary model (`google/gemma-4-31b-it:free`) sometimes guessed plausible-but-wrong tool parameter names (`chain`/`sources` instead of the schema's `chains` array); the AI SDK's default behavior on a schema-validation failure is an opaque "An error occurred." tool result with no hint of what was wrong, so the model had nothing to self-correct from — observed live producing a fully hallucinated fake product table (fabricated prices and Migros product URLs) instead of admitting the tool call failed, a direct grounding-discipline violation. Fixed with `src/agent/toolCallRepair.ts`, a deterministic `experimental_repairToolCall` pass (zero extra LLM calls) that aliases known wrong field names, coerces stringified-array values, and drops unknown keys before re-validating — plus an explicit system-prompt line naming the exact `chains` field. Re-verified live post-fix: real tool call with correct arguments on the first attempt, real Migros product data returned, and a fully grounded response where every price/product in the model's answer matched the tool's actual output exactly. Browser-verified end-to-end (headless, 390×844, real backend + real OpenRouter model + real Migros data): Chat tab renders, message send works, tool-backed product cards render distinct from prose text, no console errors. `npm run lint` 0 errors, `npx tsc --noEmit` and `npx tsc -p pwa --noEmit` clean, both builds clean, 5/5 new non-gated tests pass (1 pre-existing unrelated flaky timeout in `mcp.prodpatterns.test.ts` confirmed to pass in isolation). Not yet built (deferred, per the plan's own "genuinely open" note): the shopping-list deterministic list-detection pre-pass — early testing showed the chat model batching list items adequately without it. |
| Real-device feedback pass 2: suggestion blur, results-counter rework, ProductSheet scroll, VendorBadge collision fix | done | Five issues reported from real-device testing of the previous session's search-progress/autocomplete work. **(1) Suggestions didn't close / input kept focus after submit:** Enter-key submission (the common path with `enterKeyHint="search"` on mobile) never blurred the input, so the OS keyboard and `SuggestInput`'s dropdown both stayed open over the loading results. Added an `onBlur={() => setOpen(false)}` handler to `SuggestInput`'s `<Input>` (safe because suggestion buttons already use `onMouseDown` `preventDefault()`, so picking a suggestion never blurs first) plus an explicit `(document.activeElement as HTMLElement \| null)?.blur()` at the top of each view's `submit()` (`SearchView`/`CompareView`/`AvailabilityView`) — antigravity-mcp (gemini-3.6-flash, read-only review) suggested this over an earlier `forwardRef`/`useImperativeHandle` plan as unnecessary ref plumbing for standard blur behavior. **(2)+(3) "X/Y vendors" progress counter and ETA countdown reworked together per explicit user decision** (replace, not add-alongside): `ChainProgressEvent`/`SearchProgressEvent` gained `productsSoFar` (cumulative product count summed only over chains that responded `ok`, incremented in the same `Promise.all` closure that already tracked `respondedCount` in `searchService.ts`, passed through `server.ts`'s SSE `progress` event unchanged). `SearchView.tsx` now shows the growing result count as the primary number ("N Ergebnisse bisher (X/Y Händler)") instead of the vendor fraction. ETA countdown was hitting 0 and vanishing while the search kept running (user: "should countin with results and work and not claim done too early but better later") — fixed two ways: `server.ts`'s SSE `init` event now sources `etaMsByChain` from each chain's real measured **max** latency instead of **avg** (still real data, just a more conservative/pessimistic real number, matching the "prefer overestimating" instruction), and the countdown UI never disappears while `isFetching` is true — once remaining time hits ≤1s or all vendors have responded, it switches to a persistent "gleich fertig…" message instead of the line vanishing. **Known accepted limitation, found via a real SSE trace (not assumed):** for queries with zero direct vendor matches, `SearchService` falls through to web-search augmentation *after* the vendor fan-out completes with no progress events of its own — traced live via `birnensaft`: last chain progress at 10.9s, final `done` at 18.6s, a silent ~7.7s gap the "gleich fertig…" message just has to sit through. Typical queries with direct vendor hits (`milch`, `wasser`, `joghurt`) don't hit this path. Not fixed this session (would need a new progress-event type for the augmentation phase); accepted as an honest gap rather than faked. **(4) ProductSheet (vaul drawer) content wasn't scrollable:** a prior uncommitted attempt this session had already added `flex flex-col`/`max-h-[85dvh]` to `Drawer.Content` and `flex-1 overflow-y-auto` to the inner content div, but was missing `min-h-0` — the classic flexbox bug where a `flex-1` child defaults to `min-height: auto`, so it expands to fit content instead of being bounded by the parent, silently defeating `overflow-y-auto` (caught by antigravity-mcp's review before browser-testing, not discovered through trial and error). Fixed by adding `min-h-0`; verified in a 390×550 headless browser that the content div's `scrollHeight` (880px) now genuinely exceeds `clientHeight` (369px) and that setting `scrollTop` programmatically actually moves the scroll position (proving the box is truly scrollable, not clipped) — synthetic `WheelEvent` dispatch does not trigger real browser scroll physics (a CDP/DOM-testing limitation, not evidence of a bug; vaul's own source already checks `scrollTop`/scrollability before claiming a touch-drag as dismiss-instead-of-scroll). **(5) VendorBadge popover still overlapped the sticky header and fixed bottom nav:** a prior session's "close on scroll" band-aid didn't stop the popover rendering under/behind the header or nav on *initial open* (only after a subsequent scroll). antigravity-mcp's review correctly flagged the first fix attempt (Radix `collisionBoundary` pointed at `<main>`) as broken — `<main>` isn't itself a scroll container in this layout (the whole document scrolls), so its bounding rect grows to the full (often off-screen) page height, which would have made Floating UI's collision math worse, not better. Fixed instead with Radix's `collisionPadding` object, sourced from *real measured* pixel heights rather than guessed constants (this codebase's own `--nav-h` `ResizeObserver` pattern from the original nav-overlap fix was the explicit precedent to reuse, not repeat the guessing mistake for a second fixed element): added a matching `--header-h` `ResizeObserver` measurement in `App.tsx`, and `VendorBadge.tsx` reads both custom properties via `getComputedStyle` to build `collisionPadding={{top: headerH+8, bottom: navH+8, left:12, right:12}}`. Browser-verified in two stress positions (390×844): a trigger placed 14px above the bottom nav's top edge correctly auto-flipped the popover upward and rendered fully clear of the nav; a trigger just below the sticky header rendered fully clear of the header with no flip needed. Second opinion from antigravity-mcp (gemini-3.6-flash, read-only — instructed not to edit any files) reviewed the plan for all five fixes before implementation. 850 tests pass (6 pre-existing timeouts in `mcp.prodpatterns.test.ts`'s live-network `search_products`/`compare_prices` cases confirmed as a pre-existing 5000ms-default-timeout-vs-real-vendor-latency flake, not a regression — same 104 tests all pass at `--testTimeout=30000`), lint 0 errors (23 pre-existing warnings, unchanged), both builds clean. Browser-verified live end-to-end (headless, 390×844 and 390×550, real backend hitting real vendor APIs). |

## Next tasks (HISTORICAL — superseded by "Open items" at the top of this file)

0. ~~Deploy: Pi/VPS + Cloudflare Tunnel~~ — done via Raspberry Pi + Tailscale Serve (not Cloudflare Tunnel) + Gluetun/ProtonVPN; see tracker row above
0b. Web-search provider: ~~Google Custom Search JSON API is CLOSED to new customers~~ — multi-provider integration done (SerpAPI, HasData, Searlo, Firecrawl); Google CSE stays as dormant code for legacy accounts
0c. ~~Known gap: web-augmented search has no typo/fuzzy tolerance~~ — done 2026-07-24; see "SerpAPI spelling-fix retry" tracker row below
0d. ~~Known gap: per-store availability checks re-search each vendor's own (weak) search engine by raw query text~~ — done 2026-07-24; see "Per-product availability passthrough" tracker row below.
0e. ~~Web search provider chain rework (decided 2026-07-25)~~ — done 2026-07-25; see "Web search provider
    chain rework: DDG-primary, paid providers opt-in only" tracker row below.
0e. ~~Known gap: reverse-geocode only corrects the PLZ centroid, not raw-GPS precision~~ — done 2026-07-24; see "Raw GPS precision threaded end-to-end" tracker row below
0f. ~~PWA design pass~~ — done (see tracker row above); sticky/blurred header, blue brand accent (kept distinct from semantic green), native system font stack all shipped and browser-verified.
0g. ~~Map view mode for the availability screen~~ — done (see "Availability location pill + map view" tracker row above); location pill lives only in `AvailabilityView` (Search tab never needed location), list↔map toggle uses `leaflet`/`react-leaflet`, verified in headless browser and on a real Android phone via adb
1. ~~Run `RUN_CONTRACT_TESTS=1 pnpm test:contract` to validate real API endpoints~~ — done 2026-07-24; `test:contract`/`test:live` scripts were silently broken on Windows (embedded POSIX `VAR=1 cmd` syntax `npm run` on Windows resolves via cmd.exe, which errors on `VAR=1` prefixes; unquoted `**` glob args were also being handled inconsistently across shells and vitest's CLI treats positional args as substring filters, not globs, so the glob string matched nothing once a shell didn't expand it) — fixed via `cross-env` + plain substring filters (`contract.test.ts` / `.live.test.ts` / `.integration.test.ts`); all 5 contract test files (Migros/Coop/Lidl/Volg/Ottos) pass against real live endpoints
2. ~~Run `LIVE_SOURCE_TESTS=1 pnpm test:live` to validate live adapter smoke tests~~ — done 2026-07-24 (see above); all 7 live adapter files (all 8 chains) pass, confirming every chain integration is currently reachable and healthy
3. ~~Add richer store geospatial filtering (distance/radius)~~ — nearest-first sort done (0d above covers the remaining per-product accuracy gap)
4. ~~Implement Migros/Coop promotions endpoints~~ — Migros done 2026-07-24 (genuine live campaign feed); Coop investigated and deliberately deferred — no reliable promotion date-range source found. See "Migros promotions endpoint" tracker row below.
5. Shopping-list enricher / grounded resolution agent — scope revised, then put through five review passes (widened research + antigravity-mcp gemini-3.6-flash) before landing on ship-ready decisions same day: this is one capability of a general **PWA chat agent** (`docs/active/CHAT_AGENT_ARCHITECTURE_PLAN.md`), which reuses the existing `listTools()`/`executeToolCall()` MCP tool-dispatch layer directly (no new duplication of search/compare/availability logic). Model choice was corrected twice after owner pushback caught an incomplete comparison — first pass checked only 2 of ~17 free OpenRouter models, second pass's "independent" antigravity research suggested six models that turned out to all be paid now on live verification; final comparison checked all 17 free models with real published benchmarks: primary `google/gemma-4-31b-it:free` (76.9% Tau2, 86.4% agentic tool-use — strongest of the pool, plus genuine German/French/Italian multilingual relevance), fallback `nvidia/nemotron-3-super-120b-a12b:free` (61.15% TauBench-V2 average, same benchmark lineage as the primary), emergency fallback `openai/gpt-oss-20b:free`. Framework choice (Vercel AI SDK + official OpenRouter provider) was compared against Mastra and LangChain.js and holds up given this app's already-stateless/single-tool-loop architecture; confirmed to run standalone outside Next.js/Vercel hosting (relevant for this app's Pi/Docker self-hosting). Testing widened from two layers to three: unit (`ai/test`'s `MockLanguageModelV3`), a golden-eval Vitest fixture set (was missing entirely — catches system-prompt regressions neither mocks nor a single smoke test would catch), and one opt-in live smoke test. Shopping-list-specific resolution (RESOLVED/AMBIGUOUS/UNRESOLVED/DEGRADED status contract, quantity parsing, deterministic store-optimizer, in `docs/active/SHOPPING_LIST_AGENT_PLAN.md`) simplified to a 0-LLM-call deterministic pre-pass + retrieval, with ranking/matching folded into the chat agent's own turn using the already-decided model. `OPENROUTER_API_KEY` confirmed present at Windows user scope and funded (1000 req/day cap, verified live via OpenRouter's key-info endpoint). Still excludes automated login/checkout on a user's vendor account per `CLAUDE.md` — deep-links out to each vendor's own site only. **Implemented 2026-07-29** — see "Chat agent implementation" tracker row below; the shopping-list-specific deterministic pre-pass described above was NOT built in this pass (empirical result: the chosen model batches list items adequately without it in early testing) — remains a fast-follow if real usage shows otherwise.
6. ~~Remove static catalog data from default production runtime, or gate it as test/demo-only~~ — decided/verified 2026-07-24: already fully done by earlier vendor-coverage work, not previously marked complete. See "Static-catalog decision" tracker row below.
7. ~~Decide whether live product search requires an approved provider/central index instead of local runtime crawling~~ — decided 2026-07-24, reviewed with antigravity-mcp second opinion: accept current local-crawling-plus-resilience-infrastructure architecture provisionally (not as permanently closed); see `docs/active/SOURCE_PROVIDER_DECISION_RECORD.md` "2026-07-24 update" for concrete re-evaluation triggers.
8. ~~Normalize Migros nutrition data to per-100g/100ml basis~~ — done 2026-07-24; see "Migros nutrition per-100 column fix" tracker row below
9. ~~Investigate Aldi/Volg/Otto's product detail pages for size/quantity extraction~~ — done 2026-07-24; Volg + Otto's implemented, Aldi investigated and found infeasible without a fragile reverse-engineered parser — see "Volg/Otto's size extraction" tracker row below

| Real-device feedback pass: bugs + annoyances (Phase 1+2 of `docs/active/PWA_REALDEVICE_FEEDBACK_2026-07-25.md`) | done | 14 fixes from real Android-device testing of the v0.2.0 design refresh, consulted with antigravity-mcp (gemini-3.6-flash) on prioritization first. **Confirmed real bugs (not just perception):** (1) `MetricsCollector.loadSnapshot()` existed but was never called at boot in either `index.ts` or `web/server.ts` — worse, `web/server.ts`'s collector was constructed with no `cacheDirectory` at all, so `persistSnapshot()` silently no-opped every time; fixed both, plus added `latencyRaw` to the persisted snapshot (previously only computed avg/max were saved, not restorable raw samples) — new test proves latency survives a simulated restart. (2) `App.tsx` unmounted the previous tab's view component on every switch (`AnimatePresence mode="wait"` swapping `VIEWS[tab]`), discarding in-progress search/compare/availability state; fixed by keeping each view mounted permanently after its first visit and toggling with a `hidden` class (same pattern as the earlier Leaflet list/map fix), dropping the now-incompatible unmount-based transition. (3) All three query inputs (`SearchView`/`CompareView`/`AvailabilityView`) had a `useEffect(() => inputRef.current?.focus(), [])` that, combined with bug (2)'s remount-per-switch behavior, popped the mobile keyboard on every tab visit; removed (autofocus-on-load is a mobile anti-pattern regardless). (4) PWA manifest `theme_color`/`background_color` (`vite.config.ts`), the favicon (`pwa/public/icon.svg`), and the generated app icons (`scripts/generate-pwa-icons.mjs`) were all still hardcoded to the original emerald-600 (`#059669`) from before either the blue or brass design passes — never updated through either rebrand; regenerated. `index.html`'s `theme-color` meta was also still the interim blue (`#2563eb`); replaced with light/dark `media`-scoped tags matching `--color-bg`. **New feature work:** location input validation via a new `GET /api/geocode-check` endpoint (thin wrapper over the existing `resolveLocationAsync`) called client-side before firing a slow multi-chain availability search — rejects unresolvable text with a toast instead of burning a round-trip; GPS-resolved locations skip the check (already known-good). Brand accent hue shifted from ~33° (amber-orange, read as too close to Claude's own branding in testing) to ~45° (antique brass/gold) in both light/dark tokens and the three places it was hardcoded outside the token system (`ProductMap.tsx`'s popup/marker inline styles). `ProductMap.tsx` gained `touch-action: none` on its canvas container (standard fix for an embedded interactive map's touch gestures leaking into a scrollable parent — this was very likely the actual root cause behind two separately-reported symptoms: "can't scroll to see nutrition/the vendor link" and "map scrolling moves the sheet") plus a recenter-to-original-bounds button. Toast notifications rebuilt on `sonner`'s `toast.custom()` with a bottom position (`offset` pinned above `--nav-h`), a CSS `scaleX` receding progress bar keyed to each toast's `duration`, and swipe-to-dismiss (free from sonner's built-in bottom-position swipe direction, no extra code). Skeleton loaders switched from Tailwind's `animate-pulse` opacity blink to a moving-gradient shimmer (`@keyframes skeleton-shimmer`), also fixing an unrelated pre-existing bug where `Skeleton` hardcoded `bg-zinc-200 dark:bg-zinc-800` instead of the design-token surface colors. `SearchView`'s 2-column vertical product-tile cards replaced with `AvailabilityView`'s horizontal ticket-card format (thumbnail + info stack + barcode footer) for visual consistency between the two primary result views, per explicit user preference for the Availability format. `AvailabilityView`'s location-editing-row ↔ compact-pill transition wrapped in a `motion.div layout` + `AnimatePresence` cross-fade so the height delta between the two states animates smoothly instead of an instant jarring resize. **Explicitly deferred, not attempted this pass** (flagged as needing user discussion, not blindly implemented): replacing the DuckDuckGo/multi-provider web-search chain with "whatever agent CLIs use" — not directly actionable for a headless backend (that's an agent-side capability, not a callable API); antigravity's alternative suggestion (self-hosted SearXNG on the Pi) was relayed but not built. Client-side instant re-filtering of already-fetched results (only in-stock/open-now/max-price are safe as pure client filters per antigravity's read; chain selection would need "always fetch all chains" first) and query/location autocomplete were scoped out of this pass as Phase 3 (bigger, needs its own design pass) — see the feedback doc's Triage section. 720 tests pass (1 new: metrics latency round-trip), lint 0 errors (23 pre-existing warnings, unchanged), `tsc -p pwa` clean, both builds clean. Browser-verified dark+light mode (headless, 390×844): tab-switch state persistence (identical result set + elapsed time after a round trip), autofocus absence (`document.activeElement` stays `BODY` after a tab switch), new ticket-style Search cards, brass color rendering as gold/bronze not orange, Status tab bottom spacing, location validation rejecting nonsense input with the redesigned bottom toast + progress bar, valid-location path unaffected, ProductMap recenter button, and full nutrition/ingredients/link rendering in the Availability-opened sheet. **Known verification gap:** headless Chromium's `dvh` unit doesn't reproduce a real phone's chrome-constrained viewport, so the exact "map swallows scroll on a cramped real screen" repro couldn't be visually confirmed in this environment — the `touch-action: none` fix is the industry-standard solution for this bug class, applied with high confidence but not pixel-verified against the original failure condition. |

| Chat agent timeout fix + location-aware availability (2026-07-30) | done | Production failure on real device (`Tool "search_products" exceeded 8000ms timeout`) root-caused via direct Pi testing: `SearchService`'s `Promise.all` fan-outs (search/promotions/find-stores/availability, `src/services/searchService.ts`) and `PriceComparisonService.comparePrices` had no per-adapter timeout, so one cold-start-slow chain (Migros via Playwright+VPN, observed 17-18s) blocked the whole call past the chat agent's fixed 8s tool timeout, discarding already-arrived fast-chain results. Added `raceWithTimeout`/`ADAPTER_SOFT_TIMEOUT_MS` (6000ms, new `src/util/timeout.ts`) to every adapter fan-out in both services; raised `TOOL_TIMEOUT_MS` backstops (`search_products`/`search_promotions`/`lookup_store_product_availability` to 10000ms, `compare_prices` to 12000ms) and `chatAgent.ts`'s `REQUEST_TIMEOUT_MS` to 30000ms. **Second bug found only via mandatory manual browser verification** (not caught by any automated test): `search_products`'s web-search augmentation step (`WebProductSearchService` call for weak/uncovered chains) had *no* timeout at all — a live "Wo gibts sumach" browser test hung indefinitely past 90s with no error surfaced, because the tool's own 10s backstop expired but the underlying provider call kept running unbounded. Fixed with a second soft deadline (`WEB_SEARCH_SOFT_TIMEOUT_MS` = 8000ms) around that call and raised `search_products`'s `TOOL_TIMEOUT_MS` to 16000ms to cover both stages sequentially. Added `lookup_availability_by_location` and `set_chat_location` MCP tools (`src/tools/handlers.ts`, registered in `src/agent/tools.ts` and `src/agent/toolCallRepair.ts`) — the chat agent previously had no tool for the PWA's own "what's near me" flow (`SearchService.lookupAvailabilityByLocationProductsFirst`). Location handling is conversational-only (agent asks once, only when a location-based tool needs one and none is known yet, never proactively at chat start): `set_chat_location` resolves via the existing `resolveLocationAsync`; PWA client (`pwa/src/lib/chatHistory.ts`, `pwa/src/components/ChatView.tsx`) watches for that tool reaching `output-available` and persists the result as a new `activeLocation` IndexedDB key (chat-scoped only, explicitly not shared with `AvailabilityView`'s own location state), sent with every `POST /api/chat` and folded into `chatAgent.ts`'s system prompt per-turn; a newer `set_chat_location` call simply overwrites it. A live-browser follow-up test also caught `lookup_availability_by_location` defaulting to querying all 7 chains sequentially (extremely slow, 20s backstop hit) — fixed by defaulting to `['migros','coop']` when the model omits `chains`, matching `AvailabilityView`'s own default and `lookupAvailabilityByLocation`'s existing convention; raised its `TOOL_TIMEOUT_MS` to 25000ms. All three flows (timeout resilience, location-ask, location-override-mid-conversation) browser-verified end-to-end against the real local server (`npm run start:web` + `stealth-browser-mcp`) with the real OpenRouter model, not mocked. 766 tests pass (8 new: adapter/web-search-hang timeout cases, tool dispatch for both new tools, activeLocation system-prompt injection), lint 0 errors (39 pre-existing warnings, unchanged), build clean. Separately noted but explicitly not fixed this pass (user deferred): Aldi "Protein Milch" search returns bread not milk — real product lives on the unbuilt aldi-now.ch storefront, plus an ingredient-text AND-match false-positive in `matcher.ts`/catalog FTS5; see memory. |

| Outline-button contrast fix + two-segment border loading animation (2026-07-30) | done | User reported the "search all vendors" fallback button (`SearchView.tsx`'s empty-state CTA) was nearly invisible. Root cause: the `Button` component's `outline` variant (`pwa/src/components/ui/button.tsx`) set `bg-surface` with no border at all — indistinguishable from its own parent card, which also uses `bg-surface`, in dark mode. Fixed by adding a real border via a new `--color-outline-border` token (`color-mix` against `--color-ink`, self-adjusting for light/dark since `--color-ink` already flips per theme); affects all 7 existing `outline` usages app-wide, not just the reported instance. Separately, replaced the pulsing whole-border box-shadow "glow" on loading buttons (`button-loading-ring`, disliked by the user as "shining") with a two-segment traveling border trace: an absolutely-positioned SVG `<rect>` with `pathLength="100"` and `stroke-dasharray="15 35"` (two dashes, evenly spaced) animating `stroke-dashoffset`, using per-keyframe `cubic-bezier` easings (0%/35%/70%/100% asymmetric split) researched via antigravity-mcp (gemini-3.6-flash) for organic non-constant speed without looking jittery, with matched entry/exit velocity so the infinite loop never visibly hitches. SVG chosen over a conic-gradient/mask approach specifically because a conic sweep distorts unevenly on a non-square rounded rect (fast across long edges, slow through corners) while an SVG path's dash motion stays uniform regardless of aspect ratio. Reuses the existing global `prefers-reduced-motion` rule (freezes to a static two-dash highlight) with no extra code needed. Both fixes browser-verified against the real running local server: the outline button's new border confirmed visible via screenshot in `SearchView`'s zero-results empty state; the border-trace animation confirmed actively running via `getComputedStyle` during a real in-flight search (`stroke-dasharray`/`animation-name`/`animation-duration`/live-changing `stroke-dashoffset` all correct), since a static screenshot alone can't prove motion. `tsc -p pwa` and `vite build` both clean; no backend files touched so the backend test suite is unaffected. |

| Real-device feedback round 3: plan + Phase 1 (2026-07-30) | plan done; Phase 1 done | 13 new issues reported from real-device use with screenshots. All 13 root-caused in code and written up in `docs/active/PWA_UX_FIX_PLAN_2026-07-30.md` (7 phases), researched with antigravity-mcp (`gemini-3.6-flash`) on UX best practice, fan-out architecture, relevance ranking, and the loading indicator. **Three headline root causes:** (1) the loading border animation *never worked* — `calc()` is invalid inside SVG `<rect>` `width`/`height` **presentation attributes**, so the rect collapsed and drew a stub in one corner; the previous session's verification confirmed the *animation* was running (live `stroke-dashoffset`) but never checked the *geometry*, so it shipped broken — plus `stroke="currentColor"` resolved to the near-black label colour on gold. (2) "the cache does nothing" is not a cache bug: adapters do cache (6-24h TTL, real short-circuit), but `searchService.ts`'s `Promise.all` over a global 6000ms `raceWithTimeout` means **one unhealthy chain sets a ~6s floor on every search**, warm or cold, and the client's react-query cache is memory-only so a refresh always refetches. (3) "Sihlcity geht nicht" is a one-line config bug: `src/util/geo.ts` queries GeoAdmin with `origins: 'zipcode,gg25'` (ZIPs + municipalities only), so malls/POIs/streets can never resolve — while gibberish passes because `checkLocation` only runs on submit and fails open. Owner decisions recorded: keep the border animation but fix it properly (rejecting antigravity's 7-segment replacement), and start with Phase 1. **Phase 1 shipped** (issues 1, 2, 5, 6, PWA-only — zero backend files touched): chat pane restructured from a `sticky` composer sitting in page flow (which only pins once the page scrolls — with a short chat it sat 457px above the nav and drifted down as messages accumulated) to a `h-[calc(100dvh-var(--header-h)-var(--nav-clearance))]` flex column with `min-h-0 flex-1 overflow-y-auto` messages and the composer as a `shrink-0` flex child; new global `--nav-clearance: calc(var(--nav-h) + 1rem)` token replaces `paddingBottom: var(--nav-h)` (which equalled the nav height *exactly*, hence zero gutter on every tab — Status/Compare only looked right because they carried an ad-hoc `pb-4`, now removed), applied per-view with Chat excluded since it sizes itself; active tab moved from `useState` into the URL via new `pwa/src/lib/useTabState.ts` (`?tab=`, `pushState`/`popstate`) so a reload restores the tab and Android's back button walks tabs instead of closing the PWA; and `Unterhaltung löschen` no longer calls `window.location.reload()` — it uses `useChat`'s `setMessages([])`, which was the single line causing both the tab reset *and* the wiping of every other tab's state. `interactive-widget=resizes-content` added to the viewport meta so the Android keyboard shrinks the layout viewport rather than covering the composer. Browser-verified against the real running server at 412x915 with the service worker cleared: composer gap to nav 457px → 16px; view padding 60.61px (== nav height) → 76.61px (nav + 16px gutter), confirmed at max scroll; `?tab=status` deep-link, `pushState` on tab change, single-step back button (`?tab=compare` → `?tab=status` with the tab correctly restored), and reload-keeps-tab all pass; clearing a seeded conversation left a `window` sentinel alive (proving no reload), cleared the messages, kept the Chat tab, and left the Search tab's typed input intact. 766 tests pass, lint 0 errors (39 pre-existing warnings, unchanged), `tsc -p pwa` + `vite build` clean. **Known flake (pre-existing, not a regression):** `mcp.prodpatterns.test.ts > search_products > returns products for a basic query across all chains` intermittently times out at 5000ms under full-suite load — a live-network test racing the very 6s fan-out floor described above; passes in isolation and on re-run, and should stop flaking once Phase 4 lands. |

| Real-device feedback round 3: Phase 2 — input & visual polish (2026-07-31) | done | Issues 3, 9, 10 of `docs/active/PWA_UX_FIX_PLAN_2026-07-30.md`; PWA-only, zero backend files touched. **Issue 3 — the loading border trace, and a correction to the plan's own root cause.** The plan (and the memory written alongside it) blamed `calc()` inside the SVG `width`/`height` *presentation attribute*. That is wrong: Chrome does honour it. Measuring the live DOM mid-search showed the button at 468×44, the overlay `<svg>` at **16×16**, and the rect at 14×14 — i.e. `calc(100% - 2px)` was resolving correctly, against a viewport that had itself been shrunk. The actual culprit is the `Button` base class `[&_svg]:size-4`, a descendant-selector rule (specificity 0,2,1) that outranks the plain `w-full h-full` (0,1,0) on the loading overlay and sizes it like an icon. Fixed by excluding the overlay from that rule (`[&_svg:not(.loading-trace-svg)]:size-4`, verified present in the emitted Tailwind CSS) and moving geometry into `.button-loading-border` as CSS geometry properties, which is the more robust place for it regardless. Also dropped `stroke="currentColor"` — on the gold `default` variant it resolved to `--color-brand-ink` (#1b1206), the near-black label colour, i.e. the reported dark smudge; each variant now sets a `--loading-trace` custom property (warm white on the solid gold fill, `--color-brand` on surface-backed variants; no `outline`/`ghost` button currently passes `loading`, so that branch is defensive rather than exercised). **Issue 9 — clear affordance.** `Input` gained an opt-in `onClear` prop rendering a 36×36 X inside the trailing edge, with `pr-11` reserved for the field's whole lifetime so it never shifts on first keypress; `onMouseDown` is default-prevented so the click doesn't blur first (which would close the suggestion dropdown and collapse `AvailabilityView`'s location editor back to its pill before the click landed). `SuggestInput` exposes it as `clearable` and refocuses after clearing, keeping the mobile keyboard up for retyping. Opt-in deliberately: `CompareView`'s numeric package-count stepper must never be emptiable, and was verified to still have no X and unchanged 16px padding. **Issue 10 — chips.** Three identical copies of the chip markup (Search/Compare/Availability) replaced by one `FilterChip` (`pwa/src/components/ui/chip.tsx`); selected state changed from a solid `bg-brand` fill to a tint (`bg-brand/15` + `border-brand` + new `--color-brand-strong` label), because seven fully-saturated gold pills sat directly above the primary CTA and out-shouted it. `--color-brand-strong` is a new token rather than reuse of `--color-brand` because brand-on-tint measures ~3.2:1 in light mode, under AA for a 12px label; the new value measures **4.82:1 light / ~7.8:1 dark**, with the border at 3.84:1 against the page. Unselected reserves a transparent border, verified to give an identical 64.14×30 box in both states. **Browser-verified** against the real running server at 412×915 with the service worker and caches cleared (a stale precached bundle silently served the previous build once mid-session and had to be caught by asserting the shipped class list in the DOM — worth remembering). Loading trace: rect 466×42 inset 1px inside a 468×44 button, `rx` 8px, stroke `rgba(255,250,240,0.92)`, `stroke-dashoffset` sampled live at -0.58/-2.82/-7.58/-15.9/-28.12/-55.69 (visibly non-constant, as intended), sibling spinner icon still 16px, **plus** a screenshot with the animation paused at a known phase showing the two dash segments on opposite long edges — per the standing rule that a running animation proves nothing about geometry. Clear button: appears only when non-empty, clears the value, keeps focus, re-disables submit, and leaves the location editor open. 766 tests pass, 48 skipped (unchanged), lint 0 errors / 39 pre-existing warnings, `tsc -p pwa` + `vite build` clean. No PWA unit tests were added: the `pwa/` tree has no test harness at all (`vitest` is scoped to `src/`), so browser verification is the coverage here, as in Phase 1. |

| Real-device feedback round 3: Phase 3 — honest progress (2026-07-31) | done | Issues 7, 8 of `docs/active/PWA_UX_FIX_PLAN_2026-07-30.md`. Both reported symptoms were the progress line making promises the response could not keep. **Issue 7 — "ETA always says ~18s, always finishes earlier."** `src/web/server.ts`'s SSE `init` sent each chain's measured **max** latency, so a single historic cold-start outlier (Migros via Playwright, ~18s) pinned the estimate at the worst case forever; the client then took `Math.max()` across chains and never revised it, so the countdown kept running against the slowest chain long after that chain had replied. Now: new exported `percentile()` in `src/util/metrics.ts` and a `p75` field on `LatencySnapshot.byChain` alongside `avg`/`max`; the stream sends p75 per chain, capped at `ADAPTER_SOFT_TIMEOUT_MS` (no chain can exceed it — `raceWithTimeout` resolves there) plus a `fallbackEtaMs` for chains with no history, so the client never invents a number. `SearchView` tracks the pending-chain set in a ref and recomputes the estimate over *outstanding chains only* on every `progress` event. The countdown's dishonest end-states are gone too: instead of sitting at "gleich fertig…" (or 0s) it now distinguishes all-chains-in ("Ergebnisse werden zusammengeführt…", which is the real web-search/merge stage) from estimate-overrun-with-chains-still-out ("dauert länger als üblich…"). **Issue 8 — "counter shows 27, final list shows 12."** `productsSoFar` summed each chain's **pre-limit** result count while the response then applied `limit`. Clamped to `filters.limit` in `searchService.ts`. This stays a correct lower bound rather than a cosmetic cap: nothing downstream removes vendor products (web results merge in additively, dedupe only collapses pairs that were double-counted), so the only reduction is the limit itself — and since the raw count only grows, the clamped value is still monotonic. **Verified live, not just by unit test.** Raw SSE via curl: `etaMsByChain` now `{migros:3693, coop:1651, aldi:4507, denner:1102, lidl:6000, volg:1520, ottos:353}` with `fallbackEtaMs:6000` — versus the old ~18s max — and `productsSoFar` held at 3 for a `limit=3` query instead of climbing 3→6→9. In-browser at 412×915, sampling the rendered progress line every 200ms through a full search, the three states appeared in order and on time: `noch ~6s` counting down live → `dauert länger als üblich…` at 5.2s (6/7 chains in, estimate spent) → `Ergebnisse werden zusammengeführt…` at 6.2s (7/7 in) → results at 13.8s, i.e. the ~7.6s merge/web-search tail that previously showed a frozen "gleich fertig…". Tapping the client's own EventSource on a `milch` search gave streamed counts `[4,12,12,12,12,12,12]` against exactly 12 rendered cards (monotonic, never over-promising) — the reported failure mode reproduced and fixed. Screenshot confirms the longer German string fits on one line at 412px. 772 tests pass (6 new: p75 vs max on an outlier sample, nearest-rank `percentile()` edge cases and order-independence, clamped and unclamped `productsSoFar`), 48 skipped, lint 0 errors / 39 pre-existing warnings, `tsc` + `tsc -p pwa` + `vite build` clean. **On a scare during verification:** one full-suite run showed 6 failures in `mcp.prodpatterns.test.ts` (search_products + compare_prices). Not a regression — it was a transient live-network flake from having just hammered the vendor APIs with browser searches. Confirmed three ways rather than assumed: the file passes alone (104/104), a full-suite run against stashed-out (unmodified) code passed 766/766, and the restored changes then passed 772/772. Worth noting because this test file is live-network-backed and *will* keep doing this until Phase 4 lands. |

| Real-device feedback round 3: Phase 4 — fan-out performance (2026-07-31) | done (minus AbortController) | Issue 13 ("the cache does nothing"). Root cause was never the cache: adapters do cache 6-24h, but `Promise.all` over a flat 6000ms `raceWithTimeout` meant one unhealthy chain set a ~6s floor on every search, warm or cold. Four layers, all measured live. **(1) Per-chain timeout budgets** (`CHAIN_SOFT_TIMEOUT_MS`/`chainTimeoutMs` in `src/util/timeout.ts`) by transport class — 3s JSON API (coop/denner/volg/ottos), 5s HTML scrape (aldi/lidl), 6s headless browser (migros) — set above each class's measured p75 to cut the pathological tail, not healthy requests. **(2) `ChainHealthBreaker`** (`src/services/chainHealthBreaker.ts`), new and deliberately separate from `SourceCircuitBreaker`, which trips on *consecutive* failures and is load-bearing for the web-search provider chain — chains fail intermittently, so a consecutive counter almost never trips on the 60%-failing chain that most needs skipping. Rate-based instead: ≥5 outcomes in a 30s window, opens at ≥40% failures, 30s cooldown, half-open probe, 2 consecutive successes to close (and history cleared on close, so one later blip can't immediately re-open it). Skipped chains are excluded from latency/health recording — a 0ms skip would otherwise poison both the p75 ETA and the failure rate deciding when to close. **(3) `SearchResultCache`** (`src/services/searchResultCache.ts`), whole-query stale-while-revalidate in front of the fan-out: 2min fresh / 15min stale, 200-entry LRU, key normalised for query case/whitespace and chain order, background revalidation deduplicated per key and never passed `onChainProgress` (it would write chain events into an already-answered stream). **(4) Client cache persisted** to IndexedDB (`pwa/src/lib/queryPersist.ts`, hand-written rather than adding `@tanstack/react-query-persist-client` + `idb-keyval` to a bundle that already code-splits the AI SDK): 15min max age, 40-entry cap, 1s debounced writes, `dataUpdatedAt` preserved so react-query still applies its own staleTime. Breaker and cache are opted into in `src/web/server.ts` only — the MCP server and tests keep a deterministic every-chain-every-time service. **Two plan assumptions were wrong, and only running it showed that.** (a) "Never cache a partial result" was implemented as "reject if `metadata.sourceWarnings` is non-empty" — but the optional web-search augmentation emits its own warnings most of the time (Google 400s), so the cache rejected essentially every result and never populated; four identical live searches all re-ran the full fan-out. Completeness now means *every requested vendor chain answered ok*, computed in `SearchService` and passed to `cache.set(key, value, complete)`, since only the service knows. (b) Persisting the client cache did nothing on its own: `SearchView` hid results behind skeletons whenever `isFetching`, so a restored result was immediately blanked by the background refresh it triggered — measured 1845ms to first paint. Skeletons now show only when there is genuinely nothing to display, and a refresh over existing results shows "Ergebnisse werden aktualisiert…" instead. **Measured live end to end:** four identical searches ran 13240ms → 5319ms → 789ms → **189ms with zero progress events** (the last served wholly from the query cache; the first three correctly not cached because a chain was failing). Across seven successive queries lidl cut off at 5006-5015ms (its new 5s budget, not the old flat 6s), then after five failures the breaker opened and it cost **0ms** — with the stream's `init` reporting `lidl: 0` so the countdown no longer inflates for a chain about to be skipped (max ETA 6000ms → 3091ms). In-browser: the persisted query cache round-tripped through IndexedDB (1 entry, 11 products matching 11 rendered cards) and after a reload the same search painted in **33ms** versus 1845ms before the skeleton fix; the refreshing state was confirmed by stalling the SSE so it could be observed rather than raced (11 cards still on screen, no skeletons, hint reading "Ergebnisse werden aktualisiert…"). **Explicitly not done:** real `AbortController` cancellation threaded through `ChainAdapter.searchProducts`. It is an interface change across seven adapters plus the Playwright path — its own slice, not a performance fix, and the leak is already bounded because `SourceHttpClient` gives every HTTP request its own controller and timeout. 796 tests pass (30 new: 9 breaker state-machine cases, 12 cache cases, 4 fan-out integration cases covering breaker skip, no-breaker default, cache hit across query normalisation, and partial results being retried), lint 0 errors / 39 warnings (back to baseline after adding return types to new test helpers), `tsc` + `tsc -p pwa` + `vite build` clean. **Pre-existing failure, verified not mine:** `mcp.search.deep.test.ts > rapid sequential searches for 8 different products` times out at 5000ms; it fails identically with the changes stashed out. Same live-network flake class as the `mcp.prodpatterns.test.ts` one noted in Phase 3. |
| Real-device feedback round 3: Phase 5 — location correctness (2026-07-31) | done | Issues 4a/4b ("Sihlcity geht nicht", "any nonsense location accepted"). **The plan's diagnosis was wrong and live probing was the only way to find that out.** §2.6 assumed `origins: 'zipcode,gg25'` merely excluded malls and that adding `address`/`gazetteer`/POI origins would make Sihlcity resolve. It does not: Sihlcity is absent from *every* GeoAdmin origin and from its `featuresearch` layers (GWR, street register) too — verified live. The real defect is worse than "not found": GeoAdmin's SearchServer fuzzy-matches without ever signalling "no good match", so `resolveLocationAsync` taking `results[0]` answered "Sihlcity" with the village **Saules (BE), 100 km away**, and `/api/geocode-check` accordingly reported it *valid*. The user-visible symptom was never an error — it was a silently wrong town. Three parts. **(1) Relevance guard** (`isRelevantLabel` in `src/util/geo.ts`): every query word of 3+ characters must find a partner token in the result's label, by prefix or small edit distance so "Zuerich"→"Zürich" still resolves; a postal code may go unmatched *only if* a word matched, because municipality entries carry no ZIP and "8001 Zürich" legitimately answers "Zürich (ZH)" — when the query is all digits the code must match exactly. Reverse-prefix matching is length-floored, without which "Glatt" satisfies "Glattzentrum". **(2) Staged origins, not a widened origin set.** Origins are queried in ordered separate requests (`zipcode,gg25` → `gazetteer` → `address`) because GeoAdmin gives the result list to whichever origin matches most eagerly, so a broader set *hides* the better answer: with `address` present "8001 Zürich" returns 30 buildings on Zürichbergstrasse and no postal-code centroid at all, even at limit=30; with `gazetteer` present the same query returns only place-name entries. The common ZIP/city case still costs one request. **(3) POI fallback** via OpenStreetMap Nominatim, reached only when all GeoAdmin stages miss — it is the only source that has Sihlcity (47.3579, 8.5229) and Glattzentrum. Its usage policy is honoured: identifying User-Agent, ≥1.1s between requests (blocking for user-initiated resolution, non-blocking for keystroke-driven autocomplete so no queue of stale requests builds), results cached, same relevance guard applied to POI names. Client side: location now validates **on blur** via composed `onBlur` in `SuggestInput` (a caller handler previously replaced the internal one and would have left the dropdown stuck open), showing `aria-invalid` + an inline message, and a confirmed-invalid location can no longer be collapsed into the accepted-looking pill. Validation still fails open when the service is unreachable. **Verified in-browser** (Playwright, 420×900): "asdfghjkl" → red ring, inline error, "Fertig" disabled, pill blocked; "Sihlcity" → clean, suggests "Sihlcity (Zürich)", collapses to a pill; end-to-end "Milch" at Sihlcity returned 10 results in 13.8s led by **Coop Supermarkt Zürich Sihlcity, Kalanderplatz 1, 8045 Zürich** plus Migros Brunaupark/Bahnhof Enge/Wollishofen. 11 new tests (39 in `geo.test.ts`), 807 pass, lint 0 errors. Pre-existing flake unchanged: `mcp.search.deep.test.ts > rapid sequential searches` times out at vitest's 5000ms default and passes in 6.15s at `--testTimeout=30000` — tracker item 7, not a code bug. |
| Search-relevance golden set (2026-08-01) | done | The gate that had to exist before Phase 6 could start. `src/eval/`: 51 labelled queries in 5 linguistic buckets, scored over **frozen candidate pools** captured from the live 7-chain fan-out (1163 products) so the number moves when the ranker moves, not when a vendor has a bad afternoon. Runs offline in `npm test`, ~1s. Full rationale in `src/eval/README.md`. **Three things only building it could reveal.** (1) **Neither reported defect reproduces.** "Milchdrink UHT" returns 12 Milchdrink products, "Protein Milch" returns 19 protein drinks, and the local FTS5 catalog agrees. This is *not* a fix: `git log` shows `matcher.ts` last changed 2026-06-19, six weeks before the defects were reported, so no relevance logic changed in between. Aldi now returns **zero** results for "Protein Milch" — the bread is gone because Aldi's results are gone. Likeliest causes are vendor churn or the per-chain timeout from Phase 4 dropping the adapter before its results can be ranked. Both stay armed as `gate` queries; `knownGateViolations` is empty because claiming known-breakage that does not reproduce would be a lie the ratchet then enforces. (2) **The brand bucket scored 0.575 against results that were correct.** Migros and Aldi put the brand in `brand` and only the variant in `name` — a Toblerone bar is literally named "Crunchy Almond", Aproz water is "Gazéifiée". Judging on name alone (my first cut, to keep ingredient text out) marked all of them irrelevant. Judgement now covers name + brand and excludes category/tags structurally — `ScoredProduct` has no such field, so no later edit can quietly start reading ingredient text. Brand bucket went 0.575 → 1.0. (3) **The headline metric was flattering itself.** P@5 0.991 excluded the 5 queries that returned nothing at all. On antigravity's challenge (correct, and I had half-flagged it myself), the report now carries both pairs: ranking P@5 **0.991**/MRR **1.0** over answered queries, and end-to-end P@5 **0.894**/MRR 0.902/**coverage 0.902** over all of them. Coverage is gated separately, because a query-understanding change that stops matching hard queries would otherwise *raise* the ranking average by removing them. **Design:** labels are folded regex patterns not product IDs (IDs are per-chain and churn, so an ID-keyed corpus rots silently); it is a **ratchet, not a target** — a new gate violation fails the build and so does a stale allowlist entry. **Gate proven to fire**, not assumed: injecting an ingredient-text regression into `calculateMatchStrength` turned the build red (MRR 1 → 0.989); restored and re-verified green. 28 new tests (23 for the scorer itself — a silently wrong instrument makes every number in CI meaningless while looking green). 836 pass, lint 0 errors / 39 warnings (baseline). **Open, found by the harness:** coverage 0.902 — `geriebener-kaese`, `glutenfreies-brot`, `roter-thai-curry`, `lait-entier`, `jus-orange` return nothing from any chain. Both French queries fail in a ~23% francophone country. Inflected adjectives ("geriebener" vs "gerieben") suggest AND-matching without stemming. Not covered: the web-search augmentation path. |

## Decisions

- Development MCP scope is minimal: `context7`, `context-mode`, optional `antigravity-mcp`
- No mobile/firebase/account MCP tooling in V1 development config
- `pnpm` broken under Node 20.20.0 with corepack pnpm 11.0.9 — use `npm run` for all scripts
- Static adapters power V1; `MigrosAdapter` HTTP adapter kept as future-ready implementation
- Partial adapter failure (HTTP adapter) returns whatever succeeded; all-fail returns error
- Static catalog product search defaults to balanced deterministic matching; callers can request `matchMode: "literal"` for strict token matching
- `compare_prices` defaults to pack-price ranking for compatibility; callers can request `comparisonBasis: "unitPrice"` for normalized unit ranking
- Verification on 2026-05-18: live MCP follow-up regression cases passed; `npm run lint`, `npm test -- --run`, and `npm run build` passed before and after the real-data infrastructure slice
- Real-data migration must prove each chain independently; unsolved chains should return explicit source warnings instead of static fallback data
- Real-data infrastructure should remain backward compatible until a chain source audit chooses the first live-beta adapter
- Aldi product search is the first live-beta runtime path; deterministic tests use `createDefaultAdapters({ dataMode: "legacy-static" })` when they need the old static catalog behavior
- Aldi `^1`/`^2` superscript markers indicate "special buy" / limited stock, NOT sale with original price; rendered as colored badges with tooltips
- Aldi availability API: `GET https://api.aldi-suisse.ch/v2/service-point-product-availability?productSku={sku}&servicePointReference={ref}`; `productSku` is trailing 18-digit number in product URLs
- Aldi store finder: `GET https://api.aldi-suisse.ch/v2/service-points?latitude={lat}&longitude={lon}`; returns 233 Swiss stores; `id` field is `servicePointReference`
- Denner `unit.g` means kilograms, NOT grams (e.g., `0.5 unit.g` = 500g); `cleanContentSize` converts correctly
- Migros MMM stores (like Glattzentrum) have different `costCenterId` vs `storeId`; availability API requires `costCenterId`
- Migros API `openingHours[].hours[].open` can be time-only (`"06:30"`) or datetime (`"2026-06-20 07:30"`)
- Availability filters (In Stock, Currently Open) are purely client-side; `renderAvailResults()` re-renders from cached `lastAvailProducts` without server calls
